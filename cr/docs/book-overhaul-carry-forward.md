# Book Overhaul — Carry-Forward (Editorial Decisions)

*Seeded by Hewitt (orchestrator) on 2026-06-21. Each chapter reviser appends decisions that the next chapter must respect. Single source of cross-chapter truth during the overhaul. Not the final artifact — just the running notebook.*

---

## Permanent Invariants (do not relitigate)

### Voice and structure
- **Tutorial voice**: "you" for the reader, confident but specific, no breathlessness. Match the new preface (`## Preface`) which establishes voice + author posture.
- **Calculator through-line** in Ch 1–8: tokenizer (Ch 4), parser (Ch 5), evaluator (Ch 3 + onward), with a complete "calculator so far" callback at the end of each calculator chapter. **Do not break or rename the running types**: `Token`, `TokenKind`, `Lexer`/`Tokenizer`, `Parser`, `Expr` / variants, `Evaluator`. If you need to change one, document it here and the next chapter inherits.
- **🚧 roadmap callouts** are italic in-line asides, not narrative inversions. Pattern: *🚧 Roadmap: F-strings will support `{expr:.2f}` precision specifiers; today they accept only bare `{expr}`.* — never inverts the lesson.
- **No "AI at the Helm" interleaved roman/italic Bill-CR pattern**. This is a tutorial book, not Book 1.

### Idiomatic Lyric — use the preferred form everywhere

The spec usually presents both a low-level and an idiomatic form for an operation. **The book teaches the idiomatic form by default**, and only shows the lower-level form when explaining what the idiomatic form expands to.

### Out-of-band orchestrator promises (correction added after Ch 7)

Bill talks to sub-agents directly between turns (he reads at 750 wpm and frequently sends side channels the orchestrator doesn't see). If a reviser says "Bill / Hewitt told me X" and X sounds out of band, **trust the reviser**; do not call it fabrication. Bill's direct guidance overrides the orchestrator's task prompt. The Ch 7 reviser was correctly relaying Bill's "we'll fix the assert lowering this morning so the book is factually correct when it ships" promise — I mis-flagged it as fabrication and added an erroneous 🚧 to §7.2. That 🚧 will be removed after the assert lowering lands.

- **Method-call syntax for relation-generated methods.** When a `relation` (e.g. `relation ArrayList Team:roster owns [Player:team]`) generates child-iteration, child-add, child-remove, etc., use the method form: `team.append_player(p)`, `for player in team.players()`, `team.remove_player(p)`. The free-function form (`append_player(team, p)`) is the same thing under the hood (UFCS), but the book teaches the method form. The spec's relations section should confirm this preference; if the spec only shows the free-function form, the method form is still preferred — flag a spec doc gap in `book-overhaul-findings.md` and use the method form in the book.
- **Method-call syntax over free-function for stdlib types** where both exist: `s.len()` not `len(s)`, `list.append(x)` not `append(list, x)`, etc. — unless the spec explicitly recommends the free-function form for a specific operation.
- **F-strings** for any string interpolation that has more than one variable: `f"{name}={value}"` not `name + "=" + value.to_string()`.
- **`let` (immutable) by default**, `let mut` only when actual mutation happens.
- **`?.`** (auto-deref / Optional chaining) when accessing fields on an Optional class receiver, not a manual null-check + dereference.
- **`Error { msg: "..." }`** for error values — the stdlib class literal. The spec lists `new_error(msg)` as a shortcut, but the C backend doesn't lower it yet (Ch 5 reviser verified empirically; logged to TODO). Until that's fixed, use the class literal. Never bare-string-as-error (checker hole; don't exploit).
- **Lowercase `lock`** as the mutex keyword (never `Mutex` or capital `Lock`).
- **`Dict<K, V>`** from stdlib for dictionaries (never `map[K]V` — that's a removed built-in).

If a code example in the current chapter uses a non-idiomatic form, replace it with the idiomatic form unless the chapter's specific pedagogical point requires the lower-level form (in which case, leave it but add a one-line `*Idiomatic Lyric: `team.append_player(p)` does the same thing.*` callout).

### Lyric-language facts (current per spec at `cr/docs/lyric-language-spec.md`, 3218 lines, commit `8e458fb`)
- **Lyric vs `.lyric` vs lyre**: Lyric is the language. `.lyric` files are declaration-only Lyric (no function bodies). **lyre** is a separate toolchain (Go/Python/TypeScript/Lyric) that reads `.lyric` files and verifies them against implementations. CDD annotations (`why:`, `doc`, `invariant:`, `source:`, `fake:`) are **lyre features, not Lyric features** — never document them as Lyric syntax. The new preface already establishes lyre via the "A sibling artifact: lyre" subsection; chapters should not duplicate that pitch but may reference `.lyric` mode in passing.
- **Lowercase `lock`** is the canonical mutex keyword. **Do not use `Mutex` or `Lock` (capital)** — both removed (spec §Recently Removed).
- **No `defer` keyword** — removed (spec §Recently Removed). Use scope-exit destructors / explicit cleanup.
- **No `map[K]V` built-in** — non-functional. Use `Dict<K, V>` from stdlib instead (spec §Standard Library Reference, line ~2750).
- **No `cascade { body }` statement** — removed. Ownership cascade is automatic through `owns` relations.
- **No Go backend** — removed; only C backend.
- **`embed` keyword on interfaces** — still current (spec line 145 lists it under "Added in Lyric"). The multi-class-interface-redesign proposes to remove it in a future phase, but **for the book, `embed` is current Lyric**. If you mention `embed`, do not flag it 🚧.
- **Multi-class interface redesign** is committed as a separate roadmap doc (`cr/docs/multi-class-interface-redesign.md`, commit `f8b1271`). The book's existing 🚧 callouts on the redesign are correct as-is. **Do not adopt the redesign's syntax** in any chapter — current Lyric is the subject.
- **Function annotations on `.ly` source** — 🚧 roadmap. If currently shown as implemented anywhere, flag 🚧.
- **Strings are byte slices**, not UTF-8 codepoint sequences. UTF-8 handling is 🚧. Honest framing in Ch 4.
- **`error` interface**: spec §Interfaces line 1145 — `error` is a built-in interface. `new_error()` constructor replaces string-as-error (the 6/20 sweep already patched Ch 5 for this).
- **Numeric tower**: `u8`–`u64` and `i8`–`i64` implemented; `u128`/`u256` and floats 🚧 (spec §Design Lineage).
- **Auto-deref of Optional class receivers**: `x?.field` is current Lyric (spec §Auto-Deref). On null, it segfaults today; spec marks 🚧 for panic behavior.
- **`spawn` data race** known gotcha (captures by pointer). Document honestly in Ch 12.

### Reference doc note
- `cr/docs/lyric-language-reference.md` (722 lines) is the daily-driver companion. Cross-check examples against it but treat spec as authoritative when they disagree.

### Empirical verification recipe (compile + run a snippet)
The compiler emits C source, not a binary. To compile + run a snippet end-to-end:

```bash
cd ~/projects/lyric                              # must be project root for stdlib autoimport
./lyric compile /tmp/snippet.ly -o /tmp/snippet.c
gcc -std=gnu11 -O2 -w -x c /tmp/snippet.c -o /tmp/snippet_bin -I runtime -lm -lpthread
/tmp/snippet_bin
```

Notes:
- **Must `cd` to the lyric project root** — otherwise stdlib types (`StringBuilder`, `Dict`, etc.) fail to resolve with `checker: stdlib type X not found`.
- The `-o` from `lyric compile` is a **`.c` file**, not a binary. If you give it a path without `.c`, gcc will refuse to read it as C (treats as linker script); use `-x c` or rename.
- gcc flags taken from `test_lyric.sh` (the canonical test runner): `-std=gnu11 -O2 -w -I runtime -lm -lpthread`.
- Statements must be one per line — `;` is **not** accepted as a statement separator (`a; b` is a checker error).

---

## Calculator Through-Line State (Ch 1–8)

*To be filled in by Ch 1 reviser and updated by each subsequent reviser. Track: which types exist, which methods, which file names, what the running example computes by the end of the chapter.*

### After Preface
- Nothing yet. Calculator first appears in Ch 1.5 "A First Real Program: The Calculator."

### After Ch 1
- **File:** `calc.ly` — single file, no module.
- **Types introduced:** none (only primitives `f64` and `string`).
- **Functions:** `eval_simple(a: f64, op: string, b: f64) -> f64` — dispatches on `op` ∈ {`"+"`, `"-"`, `"*"`, `"/"`}; returns `0.0` for any other string. `main()` calls it four times.
- **What the calculator computes:** four hardcoded binary expressions (2+3, 10-4, 6*7, 15/3), printed via `println` + f-strings.
- **Known gaps deliberately left for later chapters:** (1) `op` as `string` permits typos → flagged for Ch 2's enum rewrite; (2) division by zero crashes → flagged for Ch 5; (3) no precedence, no parser → flagged for Ch 2+ (tokenizer Ch 4, parser Ch 5).
- **Ch 2 inheritance:** Ch 2 renames `eval_simple` → `eval` and replaces `op: string` with `op: Op` (enum `Add Sub Mul Div`). Ch 2 also previews a `Token` enum (later redesigned in Ch 4). Ch 2's opening sentence already references "the calculator from Chapter 1 takes two numbers and an operator string" — keep that hook intact.

### After Ch 2
- **File:** still `calc.ly` — single file, no module split yet.
- **Types introduced:** `Op` (enum `Add Sub Mul Div`) and `Token` (enum with variants `Number(value: f64)`, `Operator(op: Op)`, `LeftParen`, `RightParen`). Also teaching-only types that don't carry into later chapters: `Point`, `Rect`, `Color`, `Shape` (`Circle(radius: f64) | Rect(w: f64, h: f64)`), and a hand-rolled `Option` (`Some(value: Shape) | None`) used only to motivate the built-in `T?` introduced in Ch 3.
- **Functions:** `eval(a: f64, op: Op, b: f64) -> f64` (replaces Ch 1's `eval_simple` with the same dispatch but typed on `Op`). Helper `op_to_string(op: Op) -> string`. `main()` iterates `[Add, Sub, Mul, Div]` and prints `f"{a} {sym} {b} = {result}"` for each.
- **What the calculator computes:** the same four binary expressions as Ch 1 (now with `a=2.0, b=3.0`), printed with operator symbol via `op_to_string`. Output: `2 + 3 = 5`, `2 - 3 = -1`, `2 * 3 = 6`, `2 / 3 = 0.666667`.
- **Known gaps deliberately left for later:** (1) division by zero still crashes → Ch 5 fixes via `Result`/`?`/`new_error`; (2) `Token` is introduced as a teaching example for enum payloads but isn't *used* by `main` yet — it's there to motivate the Ch 4 tokenizer and the Ch 5 parser; (3) `as` is permissive today (see 🚧 in §2.9); (4) `match`-as-expression branch unification is not enforced (see 🚧 in §2.3).
- **Ch 3 inheritance:** Ch 3 keeps the `Op` enum and the standalone `eval(a, op, b)` function from §2.10 (it's still called from `ExprEval.apply_top`). Ch 3 introduces the `ExprEval` *class* with two slice-backed stacks (`values: [f64]`, `ops: [Op]`) plus `push_value`, `push_op`, `pop_value`, `pop_op`, `apply_top`, `result`, and uses the built-in `T?` optional for `pop_*` returns. Ch 3 should **not** redefine `Op` or `eval`.
- **Forward-reference hazard for the Ch 3 reviser:** the *pre-edit* Ch 3 §3.3 currently says "The Token **struct** from Chapter 2" — but Ch 2 defines `Token` as an *enum*, and Ch 4 §4.4 explicitly relies on that ("In Chapter 2, we defined `Token` as an enum with payloads ... So we redesign"). The Ch 4 redesign is what introduces the `TokenKind` enum + `Token` struct pair. Ch 3 reviser should fix the §3.3 line to read "The Token enum from Chapter 2" (or drop the example, since Token isn't used in Ch 3's code). Logged in `book-overhaul-findings.md`.

### After Ch 3
- **File:** still `calc.ly` — single file, no module split yet.
- **Types introduced (carrying forward):** `ExprEval` class with fields `values: [f64]` and `ops: [Op]`. Teaching-only types in this chapter that **don't** carry forward: `Node` (linked-list demo for auto-deref), `Outer`/`Inner` (lvalue-unwrap demo, both classes now), `Counter` (visibility + external-method demo), `Point` (mut-param demo — note this is a fresh `struct Point { x: i32, y: i32 }`, distinct from Ch 2's `Point` which had `f64` fields), `Stack` (concrete-typed stack motivating Ch 6's `Stack<T>`).
- **Methods on `ExprEval`:** `push_value(self, v: f64)`, `push_op(self, op: Op)`, `pop_value(self) -> f64?`, `pop_op(self) -> Op?`, `apply_top(self) -> bool`, `result(self) -> f64?`. `apply_top` pops `b` then `a`, calls `eval(a!, op!, b!)` (the Ch 2 free function), and pushes the result.
- **Functions:** `eval(a, op, b)` from Ch 2 is still the dispatch core — `ExprEval` does not redefine it. `main()` builds an `ExprEval`, feeds it `3 + 4 * 2` token-by-token, applies `*` then `+`, and prints `3 + 4 * 2 = 11`. `op_to_string` is no longer called (the chapter has moved past the "iterate all ops" demo).
- **What the calculator computes:** one expression, `3 + 4 * 2 = 11`, with manual precedence dispatch (the caller decides which `apply_top` happens first). The point is that `ExprEval` carries state across calls, not that it parses.
- **Language features introduced and load-bearing for later chapters:** classes (heap, by-reference), methods with `self`, optionals (`T?`, `isnull`, `expr!`), auto-deref of optional class receivers, lvalue write-through (class case), external methods (`func T.method(self)`), `pub` visibility, `mut` parameters on structs, lambda values and `func(T) -> U` type syntax.
- **Known gaps deliberately left for later:** (1) precedence is still done by hand at the call site — Ch 4 introduces a tokenizer, Ch 5 a parser; (2) `Stack` is `f64`-only and gets generic in Ch 6; (3) struct-typed lvalue write-through silently drops the write today (flagged 🚧 in §3.4 and in findings); (4) `fn(T) -> U` type syntax doesn't parse despite the spec listing it — the book uses `func(T) -> U`, which works (also in findings).
- **Ch 4 inheritance:** Ch 4 already opens with "We ended Chapter 3 with a calculator that evaluates expressions — but only when we feed it values and operators by hand," which matches the §3.10 wrap-up. Ch 4 introduces strings/slices and the tokenizer that produces values for `ExprEval` to consume. Ch 4 does **not** depend on `Stack`, `Counter`, `Node`, `Outer/Inner`, or `Point` from Ch 3 — those are teaching scaffolds only.

### After Ch 4
- **File:** still `calc.ly` — single file, no module split yet.
- **Types introduced (carrying forward):** `TokenKind` (flat enum, `Number | Plus | Minus | Star | Slash | LeftParen | RightParen`) and `Token` (struct, `{ kind: TokenKind, text: string }`). **This is the redesign Ch 2 promised** — Ch 2's `Token` enum-with-payloads is replaced by this enum+struct pair. The variant names `LeftParen` / `RightParen` are preserved exactly from Ch 2 (not abbreviated to `LParen`/`RParen`) so `match` arms read the same way.
- **Functions:** `tokenize(input: string) -> [Token]` — a 70-line scanner driven by a `while pos < input.len()` loop, slicing `input[start:pos]` for number text. No `Lexer` *class*; the chapter chooses a free function because there's no per-tokenization state worth bundling beyond the local `pos`. (A `Lexer` class with line/column tracking is a natural extension for Ch 5 or later when error messages need source positions.) The chapter also exercises `make_pair() -> (i32, string)` and reuses Ch 3's `ExprEval` only by reference in §4.9.
- **What the calculator computes:** `tokenize("(5 + 3) * 2")` returns a 7-element `[Token]`. The chapter's `main()` prints each token's kind and text; it does **not** yet feed them into an evaluator. Ch 5 builds the parser that bridges `tokenize` → `ExprEval`.
- **Language features introduced and load-bearing for later chapters:** byte-level string indexing (`s[i] -> u8`), character literals (`'A'`, `'\n'`, `'\x41'`), slice expressions (`s[lo:hi]`, `s[:hi]`, `s[lo:]`, `s[:]`), slice `push`/`pop`/`+`/`for x in xs`, the `append(xs, x)` built-in, `let ref` for zero-copy views, `StringBuilder`, triple-quoted strings (`""" ... """`), f-string brace doubling (`{{`/`}}`), tuples (`(T, U)` + `._0`/`._1` + destructuring), and the `atoi`/`itoa`/`char_to_string`/`parse_float` conversion built-ins.
- **Known gaps deliberately left for later:** (1) `xs.extend(ys)` is documented in the spec as the in-place append-all method, but the compiler silently drops the call — Ch 4 §4.3 demotes it to 🚧 and uses a `push` loop / `append` built-in instead; (2) UTF-8 is 🚧 — `string` is bytes today, and the chapter says so honestly with a roadmap callout in §4.1; (3) the tokenizer skips unknown characters silently — Ch 5 adds errors at the lexer/parser boundary.
- **Ch 5 inheritance:** Ch 5's pre-edit text already references `TokenKind.LParen` and `TokenKind.RParen` in §5.6 (`Parser.parse_primary` and `Parser.expect(TokenKind.RParen)?`). **The Ch 5 reviser must rename these to `LeftParen` / `RightParen`** to match Ch 2 / Ch 3 / Ch 4. Ch 5 also calls `str_to_float(tok!.text)` — that's correct (stdlib `str_to_float: string -> f64`, distinct from the C-backend builtin `parse_float: string -> (f64, bool)`); leave it alone.

### After Ch 5
- **File:** still `calc.ly` — single file, no module split yet.
- **Types introduced (carrying forward):** `Parser` class with fields `tokens: [Token]` and `pos: i32`. Teaching-only class that does **not** carry forward: `Item` (used in the §5.4 `try`-in-a-loop demo) and `ParseError` (used in §5.5 to motivate custom error types).
- **Methods on `Parser`:** `peek(self) -> Token?`, `next(self) -> Token?`, `expect(self, kind: TokenKind) -> (Token, error)`, `parse_primary(self) -> (f64, error)`, `parse_term(self) -> (f64, error)`, `parse_expr(self) -> (f64, error)`. Recursive-descent precedence: `parse_expr` calls `parse_term` for `+`/`-`, `parse_term` calls `parse_primary` for `*`/`/`, and `parse_primary` calls `parse_expr` for parenthesized sub-expressions (mutual recursion). `expect`'s returned token is discarded by `?` at the call site in `parse_primary` — we just want the error-propagation behavior, not the token.
- **Functions:** `parse(input: string) -> (f64, error)` ties tokenizer + parser together: `let tokens = tokenize(input); let parser = Parser { tokens: tokens, pos: 0 }; return parser.parse_expr()`. `main()` iterates `["(5 + 3) * 2", "10 / 3", "1 + 2 * 3 + 4", "(5 + )"]` and prints either the result or the error for each. Output: `(5 + 3) * 2 = 16` / `10 / 3 = 3.33333` / `1 + 2 * 3 + 4 = 11` / `(5 + ) => error: unexpected token: )`.
- **What the calculator computes:** the full pipeline finally works end-to-end — `parse("(5 + 3) * 2")` returns `(16.0, null)`. The malformed `"(5 + )"` reaches `parse_primary`, which sees `)` where it expects a number or `(`, and returns an `Error { msg: "unexpected token: )" }`. The `?` in `parse_term` propagates it up through `parse_expr` and out through `parse`.
- **Language features introduced and load-bearing for later chapters:** `(T, error)` tuple-return shape; tuple destructure `let (val, err) = f()` and `let (_, err) = f()` (`_` discards); the `?` operator (works in `let`-statements, expression position, both sides of a binary expression, and inside loops — when it fires, the *enclosing function* returns, not just the loop iteration); the `error` interface (any class with `pub func message(self) -> string` satisfies it via structural subtyping); the stdlib `Error { msg: string }` class as the default concrete implementation; and `f"{err}"` for stringifying any `error` value (the f-string lowerer special-cases the error type).
- **Idiomatic error construction:** the book uses the stdlib literal `Error { msg: "..." }` everywhere. The spec lists a free-function shortcut, `new_error(msg)`, but it's checker-only — the C backend doesn't lower it, so it's demoted to 🚧 in §5.4. Logged in `~/projects/lyric/TODO`. **Ch 6+ should keep using `Error { msg: ... }` until the lowering lands.**
- **Known gaps deliberately left for later:** (1) `e.message()` directly on an `error`-typed value doesn't compile — interface dispatch for `error` isn't wired up in the C backend (TODO); the book teaches `f"{err}"` instead. Calling `.message()` on a concrete error class (e.g. `ParseError`) does work. (2) The calculator still reports errors as plain text with no source position — §5.5 motivates a `ParseError { line, col, msg }` class but the calculator's `Parser` keeps the simple `Error { msg }` form. **Ch 6's pre-edit opening claims the calculator "handles errors, and reports line and column numbers" — that's aspirational for the post-Ch 6 state; the Ch 6 reviser should either soften that opening or actually add `Lexer`-tracked line/col to the parser as a Ch 6 exercise.** (3) Bare `return` inside `main()` fails to compile (C backend emits `int main(...)` and rejects `return;`); the §5.1 `main` was refactored to `if/else` to avoid this. Logged in `~/projects/lyric/TODO`. (4) `char_is_space`/`char_is_digit` C-backend lowering is missing — Ch 4's tokenizer doesn't compile today. Ch 5's parser was verified end-to-end against a hand-built `[Token]` literal as a workaround. Logged in `~/projects/lyric/TODO`.
- **Ch 6 inheritance:** Ch 6 introduces generics. The calculator's `Parser` class and its `(f64, error)` returns remain `f64`-specific; Ch 6 may generalize the evaluator (`ExprEval<T>` for any numeric `T`) or the stack types from Ch 3, but the **parser** stays `f64` for now (parser/AST genericization is a much bigger surgery than Ch 6 is sized for). The `error` type, the `Error { msg: ... }` literal, and the `?` operator are all generic-friendly already — `(T, error)` parameterizes naturally over `T`.

### After Ch 6
- **File:** still `calc.ly` — single file, no module split yet. Ch 6 does not modify the calculator's runtime code; the parser stays `f64`-only.
- **Types introduced (carrying forward):** *none*. Every type in Ch 6 is a teaching scaffold that does not carry into later chapters: `Dog` (the `Printable` constraint demo), `Stack<T>` (illustrative generic class — see 🚧 below), and the `NumericParser<T>` interface (forward-pointer to Ch 9, not a real declaration in the running code).
- **Functions introduced (carrying forward):** *none*. Teaching-only generic functions: `identity<T>`, `first<T>`, `max_val<T: Comparable>`, `print_it<T: Printable>`, `describe(val: string | i32)`, `with_default(...)`.
- **Language features introduced and load-bearing for later chapters:** generic functions (`func name<T>(...)`), explicit and inferred type arguments (`identity<i32>(42)` vs `identity(42)`), the built-in constraints `Comparable`, `Equatable`, `Hashable`, inline-constraint syntax (`<T: Comparable>`) and `where`-clause syntax (`where T: Comparable`), user-defined interface-as-constraint (`T: Printable` via structural subtyping), generic class declarations (`class Stack<T>`), type aliases (`type StringList = [string]`), union types (`string | i32`) with exhaustive `match` and wildcard `_`, and the monomorphization mental model ("the compiler generates `identity_i32`, `identity_string`, ... at each instantiation, no vtables").
- **What the calculator computes:** unchanged from Ch 5. The chapter discusses *toward* a generic parser in §6.10 but does not modify `Parser` or `tokenize`.
- **Known gaps deliberately left for later:** (1) Generic class methods that access `self.<field>` lower to a null receiver in the C backend — the `Stack<T>` example in §6.9 is demoted to "illustrative" with a 🚧 callout. Verified empirically (`Stack<f64> { items: empty }.push(1.0)` produces C `lyric_push(&/* null value */->items, ...)` which segfaults). Logged in `~/projects/lyric/TODO` and `book-overhaul-findings.md`. Generic *free* functions work fine end-to-end — `max_val<T: Comparable>`, `identity<T>`, `first<T>`, `print_it<T: Printable>` all compile and run. (2) Untyped empty slice literal does not seed type-variable inference for a generic class constructor: `Stack<f64> { items: [] }` fails with `validateAllExprsResolved: TypeVar leak 'T'`. Workaround in the book: `let empty: [f64] = []; let s = Stack<f64> { items: empty }`. (3) The §6.10 `NumericParser<T>` interface is a *forward sketch* for Ch 9; it is presented as Lyric syntax but not instantiated, since multi-class `impl` blocks are Ch 9 material. (4) `Hashable` constraint has only `get_hash` and no `equals` — already 🚧 in §6.3, matches spec.
- **Ch 7 inheritance:** Ch 7 (testing) is independent of Ch 6's generics — it tests the Ch 4 tokenizer and Ch 5 parser, both `f64`-specific. **Ch 7's pre-edit references `TokenKind` values as `TNumber` / `TOp` (parallel naming scheme), which contradicts Ch 4's `TokenKind.Number` / `TokenKind.Plus` / `TokenKind.Star` / etc.** The Ch 7 reviser must update those constants. Ch 6 introduces no naming or shape changes that Ch 7 needs to track.

### After Ch 7

- **File:** still `calc.ly` for the calculator source; Ch 7 introduces a separate `calculator_test.ly` (compiled together via `lyric test calculator_test.ly calc.ly`).
- **Types introduced (carrying forward):** *none*. Ch 7 adds no runtime types — it tests the existing `tokenize` / `Parser` / `parse` from Ch 4 + Ch 5. The §7.5 `enum Color { Red Green Blue }` is a teaching scaffold for auto-generated enum `to_string` and does not carry forward.
- **Test functions introduced (live in `calculator_test.ly`, not in calc.ly):** `test_tokenize_number`, `test_tokenize_operator`, `test_tokenize_expression`, `test_parse_number`, `test_parse_precedence`, `test_parse_parentheses`, `test_parse_empty`, `test_parse_incomplete_expr`, `test_error_message`, plus the §7.7 suite (`test_tokenize_numbers`, `test_tokenize_parens`, `test_eval_simple`, `test_eval_nested_parens`, `test_unmatched_paren`, `test_empty_parens`).
- **Language features introduced and load-bearing for later chapters:** the `test_*` discovery convention, the `assert(cond, msg?)` and `assert_eq(actual, expected, msg?)` compiler builtins (both messages are optional in practice — verified empirically), the `lyric test [files...]` CLI shape, multi-file compilation (`lyric test test_lexer.ly ../src/lexer.ly`), auto-generated `to_string()` for enums/structs/classes (used by `assert_eq` value display).
- **Idiomatic test patterns established:** `assert(err == null, "...")` for happy-path checks; `assert(err != null, "...")` for error-path checks; `assert_eq(f"{err}", "expected message text")` to inspect error messages (not `err!.message()` — that's still 🚧 per Ch 5's interface-dispatch finding); `assert_eq(tokens.len(), N)` and `assert_eq(tokens[i].kind, TokenKind.Number)` for tokenizer assertions.
- **What the calculator computes:** unchanged from Ch 5. Ch 7 only adds verification.
- **Known gaps deliberately left for later:** (1) **CRITICAL** — the C backend currently drops every `assert(...)` and `assert_eq(...)` call on the floor; the generated function bodies for tests are empty `{}`, so every test silently passes regardless of correctness. The runtime header defines `lyric_assert` / `lyric_assert_eq` macros (with file/line and the FAIL output format from spec §Testing), but the lowerer never emits them. Logged in `~/projects/lyric/TODO`. **The Ch 7 reviser was instructed by Hewitt to write the chapter as if this works, because he intends to fix the lowering before next session.** Future chapters that lean on `lyric test` to verify their examples (Ch 13's split calculator, Ch 14's compiler tests) should re-verify once the lowering lands. (2) `assert_eq_approx(actual, expected, tol)` for float-tolerance comparisons is on the roadmap (§7.2 mentions it inline); writing a one-line helper is the current workaround. (3) Per-test timing, directory-based discovery (`lyric test -mod . pkg/...`), test filtering, parallel execution, and coverage are all 🚧 / non-goals per spec §Testing.
- **Bug discovered while verifying Ch 7 examples:** `(T, error)` tuple destructure on a *self-recursive* method call inside a method that itself returns `(T, error)` emits malformed C — the lowerer reuses the `LyricResult_double` temp directly as if it were a `double`/`const char*` pair. Repro and full diagnostic in TODO. Workaround: use `?` instead of explicit `let (val, err) = self.<recursive>()` destructure for self-recursive `(T, error)` calls. The Ch 5 calc.ly already uses `?` in this spot, so the calculator still compiles.
- **Ch 8 inheritance:** Ch 8 (Relations) is independent of Ch 7's testing infrastructure — it doesn't import any test functions or rely on `lyric test`. Ch 8 introduces a fresh `class Expr` AST example in §8.1 that does not extend the calculator's `Parser`/`tokenize` runtime; the calculator's `f64`-only parser stays as-is through Ch 8.

### After Ch 8
- The full calculator (tokenize → parse → evaluate) is complete by the end of Ch 8. Ch 8 itself does not extend or modify the calculator — it teaches relations through standalone examples (`Team`/`Player`, `Room`/`Guest`, `Parent`/`Child1`/`Child2`, `TeamA`/`TeamB`/`Player`, and a small `Program`/`Stmt`/`Expr` AST in §8.6 that's a fresh example, not a refactor of the Ch 5 `Parser`).
- **Types introduced (carrying forward):** *none*. Every Ch 8 class is a teaching scaffold. The Ch 5 `Parser`, `Token`, `TokenKind` are unchanged.
- **Language features introduced and load-bearing for later chapters:** the `relation` declaration (`relation Hint Parent:label owns|refs [Child:label]`); the four stdlib relation hints `ArrayList` (and its base `ArrayListBase`), `OwningList`, `RefList`, `HashedList`; cascade destruction via `owns`; non-cascading unlink via `refs`; **label-prefixed methods** auto-generated from the parent label (`t.roster_append(p)`, `t.roster_remove(p)` for `:roster`; `r.room_append(g)` for `:room`); **label-prefixed fields** (`roster_children`, `team_parent`, `team_index`, `room_first`, `guest_next`, ...); a class participating in *multiple* relations distinguished by label (`p.c1_append` vs `p.c2_append`); a child belonging to multiple parents with auto-unlink on cascade destruction (the `destroy_shared.ly` pattern); `final func` for pre-destruction resource cleanup (Ch 11 mentions this in passing; Ch 8 §8.7 is where it's actually taught).
- **Idiomatic relation patterns established:** **method-call form for relation-generated ops** (`t.roster_append(p)`, NOT `array_append<Team, Player>(t, p)`) — verified end-to-end against `testdata/arraylist.ly`, `testdata/ref_list.ly`, `testdata/multi_relation.ly`, `testdata/destroy_shared.ly` (which uses the method form for `RefList` already, confirming the pattern); `parent.label_children.len()` for collection size (method chain, not `len(...)`); direct field access for traversal (`r.room_first`, `cur!.guest_next`); `prog.destroy()` for whole-tree teardown.
- **Known gaps deliberately left for later / 🚧:** (1) `final func` fires twice when `.destroy()` is called explicitly on a stack-local class (workaround: omit the explicit destroy and let scope-exit drive it; logged in `~/projects/lyric/TODO`, also noted as a 🚧 callout in §8.7); (2) UAF after `.destroy()` is acknowledged honestly in §8.2 ("undefined behavior… discuss further in Chapter 11") — the slab-zero coincidence that makes `isnull(p4.team_parent)` return `true` is called out as not-to-be-relied-on; (3) the chapter does *not* claim relations prevent use-after-free statically (that's a roadmap item per spec §Memory Model, deferred to Ch 11).
- **Ch 9 inheritance:** Ch 9 (Interfaces) opens with "Chapter 8 showed what relations *do*. This chapter shows how they work." That hook is intact — Ch 9 reaches under the hood at the same `ArrayList`/`OwningList`/etc. examples Ch 8 used at the user level. Ch 9 then introduces `Graph<G, N, E>` for the multi-class interface lift, `impl` blocks (alias, FieldBind via `<->`, inline), `field` injection, `embed`, and `destructor` injection. **Ch 9's pre-edit §9.5 already shows an `ArrayList<P,C>` destructor snippet that uses `parent.children()` / `array_remove(self)` (getter-and-setter form), while Ch 8's refreshed §8.2 now uses direct field access (`self.children` / `child.parent = null`) to match the current stdlib.** Not a contradiction — both forms work inside an interface body — but the Ch 9 reviser should consider aligning §9.5's example to the direct-field-access form for consistency with Ch 8 §8.2.

### After Ch 9
- The calculator is unchanged from Ch 5/Ch 8. Ch 9 teaches interfaces through the same standalone examples it always did: `Graph<G, N, E>` (with `SimpleGraph`/`SimpleNode`/`SimpleEdge` impl), `MyList<P, C>` (with `Panel`/`Widget`), `DoublyLinked<P, C>` + `OwningList<P, C>` (the `embed` showcase), `ArrayList<P, C>`'s destructor injection, the `where ArrayList<P, C>` generic function pattern, and the `func Sym.equals(self, other)` external-method idiom. No types from Ch 9 carry into Ch 10+; the chapter is a deep dive into the mechanism behind Ch 8's stdlib hints.
- **Types introduced (carrying forward):** *none*. Every Ch 9 class (`SimpleGraph`/`SimpleNode`/`SimpleEdge`, `Widget`/`Panel`, `Folder`/`File`, `Task`) is a teaching scaffold.
- **Language features introduced and load-bearing for later chapters:** `interface` declarations with multi-class type parameters (`G, N, E` or `P, C`); `func T.method(self)` abstract method declarations bound to a type parameter; `field T.name: Type` field injection from interface into concrete classes; `destructor T { body }` destructor injection; `embed Other<P, C>` for interface composition (copies fields and destructors, not methods); `impl Iface<A, B, ...>` blocks with three mapping forms — alias (`G.nodes = SimpleGraph.get_nodes`), FieldBind (`P.children <-> Folder.items` with the `<->` bidirectional token), and inline (`P.to_string = (self) -> string { ... }`); `class T implements Iface, ...` declarative conformance marker; `where Iface<P, C>` relational constraint clause on generic functions; external method declarations (`func Sym.equals(self, other: Sym) -> bool { ... }`).
- **Idiomatic Lyric patterns established in Ch 9:** `nodes.len()` not `len(nodes)` everywhere in default-method bodies; direct field access (`self.children[i]`, `child.parent = null`) in destructors that operate on concrete-typed fields (matches Ch 8 §8.2); getter/setter form (`parent.items()`, `parent.set_items(...)`) inside interface default-method bodies, where the receiver is a type variable and the abstract field can only be reached through its auto-generated accessor; free-function call form with explicit type args (`count_edges<SimpleGraph, SimpleNode, SimpleEdge>(g)`, `add<Panel, Widget>(panel, w1)`) for invoking default methods — until the method-call form lands.
- **What's correct, verified end-to-end against `testdata/interfaces.ly` and `testdata/embed.ly`:** the §9.1 `Graph` interface + §9.2 impl block + `count_edges<...>(g)` call (output `1`); the §9.7 `count_children<P, C>(p)` constrained by `where ArrayList<P, C>` (output `1`); the §9.4 FieldBind `<->` form against `DoublyLinked<Folder, File>`; the §9.6 `embed DoublyLinked<P, C>` inside `OwningList<P, C>` (auto-injects `first`/`last`/`next`/`prev`/`parent` fields — the same `testdata/embed.ly` mechanism); the §9.8 `func Sym.equals(self, other)` external-method form.
- **Known gaps deliberately left for later / 🚧 callouts added in Ch 9:**
  - (1) **§9.2** — *Roadmap callout* added: the spec says default methods are callable in method-syntax (`g.count_edges()`), but the checker rejects that form for any default method on a user-defined interface. Free-function form `count_edges<G, N, E>(g)` is the only spelling that resolves today. Logged in `~/projects/lyric/TODO`.
  - (2) **§9.3** — entirely rewritten. The pre-edit §9.3 declared `pub func P.add(self, child: C) { ... }` inside `interface MyList<P, C>` and called `panel.add(w1)` at the call site. None of that compiles: the receiver-binding `P.add` form works as a *declaration*, but `panel.add(w1)` fails with `checker: unknown method: add`, `panel.w_add(w1)` (the label-prefixed form the spec promises) also fails, and the body's `self.items` direct-field access fails with `field items not found on type`. Post-edit §9.3 uses the only pattern that compiles end-to-end: top-level default method (`pub func add(parent: P, child: C)`), getter/setter form inside (`parent.items()` / `parent.set_items(...)`), free-function call site (`add<Panel, Widget>(panel, w1)`), plus a 🚧 callout pointing readers at the stdlib hints (`team.roster_append(p)`, `dir.files_append(f)` from Ch 8) when they want method-call ergonomics today. Both bugs logged in `~/projects/lyric/TODO`.
  - (3) **§9.5** — destructor snippet aligned to direct-field-access style (`self.children[i].destroy()`, `self.children.len() - 1`) per the Ch 8 reviser's flag. Matches the current `stdlib/std.ly` `ArrayList<P, C>` factoring (which is built on `ArrayListBase<P, C>` and uses `self.children` / `child.parent = self` directly, with `trusted` for the `ref child` / `unref child` calls). The interface body still uses `array_remove(self)` in `destructor C` because that's what the stdlib does.
  - (4) **§9.2** — `implements` callout retained: the keyword parses and is recorded by the checker but missing methods don't surface until lowering/codegen. Spec §`implements` confirms this is declarative-only today.
- **§9.10 line counts updated:** `stdlib/std.ly` is now 733 lines, 991 with `string.ly` (was 617 / 875 in the pre-edit).
- **Ch 10 inheritance:** Ch 10 (Sym and Dict) opens with "Every nontrivial program needs a hash table… we need to talk about the key" and refers back to Ch 8 + Ch 9 generically. Nothing in Ch 9 changes that handoff. The `Hashable` constraint (mentioned in §9.10's bullet list) and external-method idiom (`func Sym.equals(self, other) -> bool` in §9.8) are the load-bearing pieces — Ch 10 introduces `Sym` and `Dict<K, V>` using both. Ch 9 does **not** redefine `Hashable` or `Sym` — it only references them.

### After Ch 10
- The calculator is unchanged from Ch 5/Ch 8 and stays that way — Ch 10 teaches `Sym` and `Dict<K, V>` through fully standalone examples. The pre-edit §10.5 tried to graft a `Calculator { vars: Dict<Sym, f64> }` class onto the running calc, but (a) that doesn't compile (Dict-as-field typevar leak — see findings) and (b) it violated the "Ch 10+ uses standalone examples" rule. §10.5 is now a self-contained "Variable Bindings" example: a top-level `let vars = Dict<Sym, f64>()` in `main` plus a free function `get_var(d: Dict<Sym, f64>, name: string) -> (f64, error)`. The Ch 5 `Parser` / `Token` / `TokenKind` types are not touched and not referenced.
- **Types introduced (carrying forward):** *none*. Every Ch 10 class is either stdlib (`Sym`, `SymTable`, `Dict<K, V>`, `DictEntry<K, V>`) or a one-paragraph teaching scaffold (`NodeId` for the "implement Hashable on your own key" example). The calculator's surface area is unchanged.
- **Language features introduced and load-bearing for later chapters:** `sym(name)` interning + backtick-sym syntax (`` `name` ``) and the `Sym` class API (`get_name`, `get_hash`, `hash_key`); the `Hashable` interface (currently single-method `get_hash`); the `Dict<K, V>` class API (`set`, `get`, `has`, `remove`, `keys`); Dict literal syntax for backtick-sym and integer keys; the `permanent` class modifier (opts a class out of slab reclamation — load-bearing for `SymTable`, mentioned in passing); the `HashedList` relation hint as the under-the-hood machinery for both `SymTable` and `Dict`.
- **Idiomatic patterns established:** `d.set(k, v)` / `d.get(k)` / `d.has(k)` / `d.remove(k)` method-call form (matches stdlib `Dict.*` external-method declarations and §Idiomatic Lyric); `keys.len()` on the slice returned by `d.keys()` (the stdlib's `Dict.length()` method is **not** used in the chapter — see 🚧 below); `f"{err}"` for stringifying error values (carries forward from Ch 5); `Error { msg: ... }` literal for error construction (carries forward from Ch 5); `sym(name)` at every Dict-key boundary (the hash-once discipline).
- **What §10.5 computes (verified end-to-end against `./lyric compile` + gcc + run):** `pi = 3.14159` and `undefined variable: tau`.
- **Known gaps deliberately left for later / 🚧 callouts added in Ch 10:**
  - (1) **§10.1** — `HashedList` matches entries by `hash_key()` alone; `Hashable` has only `get_hash`, no `equals`. For `Sym` keys this is safe (the intern table enforces one-entry-per-string); for other key types, hash collisions match the wrong entry silently. Roadmap fix is `Hashable.equals` restoration (already in §Roadmap §Stdlib). Callout repeated once in §10.3 at the Dict-literals point for emphasis.
  - (2) **§10.3 Dict Literals** — the spec promises three Dict-literal key forms (backtick-sym, string-literal, integer-literal). Empirically only backtick-sym and integer-literal parse; string-literal keys (`{"NYC": 8_000_000}`) trip the disambiguator. Demoted with a 🚧 roadmap callout; the workaround for string-keyed dicts is `let d = Dict<string, V>()` + explicit `.set(...)`. Logged in `~/projects/lyric/TODO`.
  - (3) **§10.5** — `Dict<K, V>` as a field on another class is broken two ways: non-generic outer leaks the `V` typevar at the checker (`validateAllExprsResolved: TypeVar leak 'V'`); generic outer trips a monomorphizer `unmangled generic class 'Dict' reached c_backend` failure. Until both are fixed, the working pedagogy is a top-level `Dict<K, V>` plus free functions. Same bug family as the Ch 6 `Stack<T>` generic-class issue (logged in `~/projects/lyric/TODO`).
- **Spec/stdlib drift noted (no compiler bug, just doc work):**
  - Spec §Dict<K,V> methods table lists `len() -> i32`; stdlib actually defines `length()`. The book sidesteps by using `keys.len()` on the slice rather than `d.length()` or `d.len()` in any example. Logged in `~/projects/lyric/TODO` as a rename-or-doc-fix.
  - Spec §Sym shows `pub class SymTable { }` without `permanent`; stdlib `stdlib/std.ly:503` is `pub permanent class SymTable { }`. §10.1 now matches reality with a one-sentence prose mention of what `permanent` does.
- **Ch 11 inheritance:** Ch 11 (memory management) opens with a generic recap — no specific hook into Ch 10's calculator (good, since there's no Ch 10 calculator). Ch 11 will need to explain `permanent` honestly (Ch 10 now hands it off as a load-bearing keyword for `SymTable`); the pre-edit Ch 11 §11.2 dives into slab allocation but doesn't appear to cover `permanent` — the Ch 11 reviser should consider whether `permanent` deserves a short subsection or a one-line callback. Ch 11 also still references `--soa` mode; that's outside the Ch 10 reviser's scope.

### After Ch 11
- The calculator is unchanged from Ch 5/Ch 8. Ch 11 teaches memory management through standalone examples (`Point`/`Particle`, `Node` slab walkthrough, `TeamA`/`TeamB`/`Player` destroy-cascade, `Holder` escape-analysis, `Config` copy-on-assign) and a §11.8 inventory of the running calculator's costs. No types, methods, or files added or renamed.
- **Sections added / renumbered:** new **§11.5 "The Three Lifetime Regimes"** inserted to make Owned / Permanent / Refcounted explicit (Ch 10 reviser's hand-off requested a `permanent` callback; this is it, plus the missing owned and refcounted regimes the chapter previously left implicit). The "move semantics are inferred" paragraph moved here from the old §11.6 Copy-on-Assign tail so the entire lifetime-regime discussion lives in one place. Subsequent sections renumbered: old §11.5 → **§11.6 Scope-Exit Analysis**, old §11.6 → **§11.7 Copy-on-Assign**, old §11.7 → **§11.8 What the Calculator Costs**.
- **§11.4 destroy-shared example:** call sites converted from free-function form (`dll_append<TeamA, Player>(a, p)`) to method form (`a.team_a_append(p)`) per Ch 8 §Idiomatic Lyric decision. Verified end-to-end (`./lyric compile` + gcc + run): identical output to the original.
- **§11.4 UAF callout extended** with a one-line mention of `--detect-uaf` debug mode (sentinel-marked freed slots; spec §Memory Model). Bidirectional pointers, `destroys`, `mut resize` all kept 🚧 (current and correct).
- **§11.6 idiom fix:** `while i < len(temps)` → `while i < temps.len()`. Verified.
- **§11.8 rewritten:** the pre-edit inventory listed a `Calculator` class with a `Dict` field that doesn't exist in the book (Ch 10's reviser explicitly rewrote §10.5 to a standalone `Dict<Sym, f64>` example, no `Calculator` class). New §11.8 inventories what the book actually built: the Ch 5 `Parser` and its token slice, plus the §10.5 variable-bindings example as an additive bolt-on. Honest about `Parser` being refcounted (no `owns`, not `permanent`).
- **Reference counting framing:** the task brief flagged ref counting as 🚧, but the spec §Memory Model and the reference doc both present it as a current regime (the third regime, with `--rc-free` default ON and `--no-rc` as the opt-out flag). Trusted the spec. §11.5 introduces it as current Lyric with no 🚧.
- **Known gaps deliberately left for later / 🚧 already in chapter:** UAF after `.destroy()` (already prominent in §11.4); bidirectional pointers, `destroys` annotation, `mut resize`, safe iterators across destroy-during-iteration (all 🚧 in §11.4 callout); `permanent`-as-relation-target warning (🚧 in new §11.5).
- **Ch 12 inheritance:** Ch 12 (Concurrency) opens with `spawn` + `make_channel<T>` and references `spawn`'s by-pointer capture. Ch 11 §11.6's last paragraph already forward-references "`spawn` blocks, which capture variables by pointer (see Chapter 12)" — that hook is preserved. No naming or shape changes from Ch 11 affect Ch 12.

### After Ch 12
- The calculator is unchanged. Ch 12 teaches concurrency through standalone examples (spawn-with-channel-handshake, buffered/unbuffered channels, producer/consumer, select, lock, `Counter` with `guarded_by`, and a two-worker concurrent accumulator). No types, methods, or files added or renamed. The `Counter` class in §12.6 is a teaching scaffold that does not carry forward.
- **Language features introduced and load-bearing for later chapters:** `spawn { body }`, `make_channel<T>()` / `make_channel<T>(n)`, channel methods `send` / `receive` / `close`, the `channel<T>` type for parameters, `select { case x = ch.receive() => ... case ch.send(v) => ... default => ... }`, the `lock` type (lowercase, zero-init: `let mut mu: lock`) and `lock(mu) { ... }` scoped-mutex statement, and the `guarded_by(field_name)` contextual-keyword annotation on class fields.
- **Idiomatic Lyric patterns established:** method-call form for every channel op (`ch.send(v)` / `ch.receive()` / `ch.close()`, never bare `send(ch, v)`); lowercase `lock` everywhere; `let mut mu: lock` declared-without-constructor as the canonical mutex pattern; channels-by-reference is the safe shared-state primitive (channels are class pointers with internal locking, so capturing them in a `spawn` is fine despite the by-pointer-capture race); `share memory by communicating` framing (the §12.7 worker-pool example is the showcase — each worker has a local `sum`, ships its result through a channel, no locks needed).
- **Verified end-to-end (`./lyric compile` + gcc + run):** the §12.1 opener (`hello from goroutine: 42` / `all done`), the rewritten §12.3 producer (`got: 1` … `got: 5` / `producer done`), §12.5 lock (`final count: 11`), §12.6 `Counter` with `guarded_by` (compiles + runs; annotation is silently unenforced as documented), and §12.7 concurrent accumulator (`total: 21`). Channel + select testdata files (`testdata/channels.ly`, `testdata/select.ly`) also re-verified end-to-end.
- **Known gaps deliberately left for later / 🚧 already in chapter:**
  - (1) **`spawn` captures by pointer** — known data race for any captured `let mut` of a value type. §12.1 has a prominent 🚧 callout explaining the race, why channels are exempt (internal locking), the two workarounds (copy-in / use `lock` / funnel through a channel), and the roadmap intent (copy-by-value with explicit shared-mutation through channels or locks).
  - (2) **`receive()` on a closed channel returns the zero value with no `(value, ok)` signal.** §12.3 has a 🚧 callout with three workarounds (sentinel value, separate `done` channel, `Optional`-wrapping). The chapter's producer example uses option 1 — sending `1..=5` so `0` becomes a usable sentinel — and the prose explicitly names this choice. (The pre-edit producer started from `0` and the `val >= 0` loop never terminated; verified empirically by hanging on `timeout 3 /tmp/producer_bin`.)
  - (3) **`select` polls with `usleep(100)`** rather than blocking on a condvar/epoll. §12.4 has a 🚧 callout naming the ~100µs latency cost and the roadmap target (condvar/epoll). The pre-edit text said `sched_yield()` — that's wrong; spec §Concurrency line ~1696 explicitly says `usleep(100)`. Fixed.
  - (4) **`guarded_by` is parsed but not enforced** by the checker. §12.6 has a 🚧 callout explicitly stating that `c.count` access outside `lock(c.mu)` compiles cleanly today and that the roadmap item is the cross-function check.
- **Ch 13 inheritance:** Ch 13 (Modules and Packages) is independent of Ch 12's concurrency primitives — its examples are a two-package `mylib/` split. Nothing in Ch 12 changes the calculator or introduces shapes Ch 13 needs to track. If Ch 13 splits the calculator across files (which is its likely point), the `tokenize` / `Parser` / `parse` shapes from Ch 5 still apply unchanged.

---

## Cross-Chapter Decisions Log

*Append decisions that future chapters must respect. Format:*

*`## Decision (Ch N): <short title>`*
*`Decided to <X> because <why>. Next chapters: <impact>.`*

(Empty at seed time.)

## Decision (Ch 1): Calculator source file is `calc.ly`
Decided to give the running calculator a single, stable filename — `calc.ly` — starting at Ch 1.5, because every subsequent chapter compiles and runs it. Next chapters: use `calc.ly` until the calculator is split into a multi-file package (likely Ch 6 or later when classes/relations land); when that happens, document the file split in this log.

## Decision (Ch 1): Honest framing for implicit int→float widening
Decided to document implicit int→float widening in §1.2 even though the spec's §Type Casts only explicitly mentions integer-to-integer widening. Behavior is real (orchestrator-confirmed) and load-bearing for natural-looking numeric code; spec gap filed in `cr/docs/book-overhaul-findings.md`. Next chapters: rely on implicit int→float widening freely in examples; the spec will be tightened to document it.

---

## Roadmap Items the Book Should Mark 🚧

This is the consolidated list from the spec's §Roadmap and §Recently Removed sections, so chapter revisers don't have to re-derive it. If you find one of these claimed as implemented in your chapter, demote to 🚧.

### Type system (🚧)
- Numeric widths beyond u8–u64 / i8–i64 (no `u128`, `u256`, floats yet)
- Branch-type unification in if-expressions
- Restricted `as` casts (no narrowing without explicit syntax)

### Operators (🚧)
- Bitwise operator precedence fix (currently below `==`/`<`/`&&`/`||`)
- `~` operator
- Compound assignment operators (`+=`, `-=`, etc.) beyond what's implemented

### Lexer / literals (🚧)
- UTF-8 in strings (currently bytes)

### Imports / modules (🚧)
- Various import-related features (consult spec §Imports)

### Function annotations (🚧)
- `.ly` source function annotations (currently `.lyric` declaration-layer only)

### Memory safety (🚧)
- Bidirectional pointers as escape hatch
- `destroys` annotation for UAF prevention
- `mut resize` annotation
- Safe iterators across destroy-during-iteration

### Stdlib (🚧)
- `Hashable.equals` restoration (HashedList currently matches by hash alone)
- 1-to-1 relation hint
- Iteration sugar (`for x in obj` without `.iter()`)
- Planned I/O library (spec §I/O Library — Planned, line ~2951)

### Removed (do NOT teach as current Lyric)
- CDD annotations (`why:`, `doc`, `invariant:`, `source:`, `fake:`) → moved to lyre
- `lyric verify` / `lyric update` / `lyric gen` → moved to lyre
- Three-zone `.lyric` file layout → moved to lyre
- `defer` keyword
- `cascade { body }` statement
- `Mutex` / `Lock` (capital) type — use lowercase `lock`
- `map[K]V` built-in — use `Dict<K, V>`
- Go backend

## Decision (Ch 2): `Token` stays an enum in Ch 2, becomes a `TokenKind` enum + `Token` struct in Ch 4
Decided to keep Ch 2's `Token` definition as an *enum with payloads* (`Number(value: f64) | Operator(op: Op) | LeftParen | RightParen`) because the chapter's pedagogical point in §2.4 is "enums can carry data" and Ch 4 §4.4 explicitly hangs its tokenizer redesign on this earlier shape ("In Chapter 2, we defined `Token` as an enum with payloads. So we redesign..."). Next chapters: Ch 3 must say "Token enum from Chapter 2" (not "struct"); Ch 4 keeps its planned `TokenKind` enum + `Token` struct redesign untouched; the running calculator does not depend on Ch 2's `Token` at runtime, only conceptually.

## Decision (Ch 2): The hand-rolled `Option` in §2.5 is a teaching scaffold only
Decided to keep the `enum Option { Some(value: Shape) | None }` in §2.5 as a deliberate scaffold for teaching nested patterns *before* the reader has met Lyric's built-in `T?`. The forward pointer is to **Chapter 3** (where `T?` lands), not Chapter 6 — Ch 3 already uses `f64?` heavily, so promising the reader they'll have to wait until Ch 6 was misleading. Next chapters: do not redefine `Option`; do not use the §2.5 `Option` in any later calculator code.

## Decision (Ch 2): Bare `let p = Point { 10, 20 }` is rejected — positional struct literals require an expression-depth context
Decided to fix §2.1 to match parser reality: positional struct literals only work inside parens, function arguments, or list literals. The previous claim that a bare `let p = Point { 10, 20 }` "works because it follows `=`" was a fabrication contradicted by `testdata/positional_struct_lit.ly`. Next chapters: when introducing new structs in code examples, use the named-field form (`Point { x: 10, y: 20 }`) for bare `let` assignments; positional is fine inside `(...)`, arg lists, and `[...]`.

## Decision (Ch 3): Lvalue write-through is taught on class-typed inner only; struct case is 🚧
Decided to rewrite §3.4's lvalue-unwrap example so the inner type is a `class Inner { value: i32 }` rather than a `struct Inner { value: i32 }`. Empirical test against the current compiler: `o.data!.value = 42` on a struct-typed inner Optional silently writes to a temporary copy and `println(o.data!.value)` still prints `0` — confirmed end-to-end (compile + run) in the Ch 3 revision session. Spec §Lvalue Unwrap and Write-Through (line ~1543) shows the struct version as if it worked; that's a spec/compiler gap. Logged in `book-overhaul-findings.md` and in `~/projects/lyric/TODO`. Next chapters: model "mutable inner state" with a class, or pull-mutate-replace for structs; do not show struct-typed lvalue write-through as a current Lyric pattern.

## Decision (Ch 3): `Point` is re-introduced as a fresh `i32` struct in §3.7 (not Ch 2's `f64` `Point`)
Decided to leave §3.7's `struct Point { x: i32, y: i32 }` mut-param demo as-is, even though Ch 2 already defined a `Point` with `f64` fields. The two `Point`s are pedagogical scaffolds in independent chapters — neither is carried into the calculator. Next chapters: if `Point` is reintroduced again, declare it fresh; do not assume either Ch 2's or Ch 3's `Point` is in scope. The calculator through-line uses `ExprEval` (class), `Op` (enum), `Token` (enum, Ch 2 / redesigned Ch 4), and `eval` (free function) — no `Point` at all.

## Decision (Ch 4): `Token` redesigned into `TokenKind` enum + `Token` struct; paren variants stay `LeftParen`/`RightParen`
Decided to land the redesign Ch 2 flagged: `Token` becomes a `struct { kind: TokenKind, text: string }` paired with a flat `TokenKind` enum (`Number | Plus | Minus | Star | Slash | LeftParen | RightParen`). The split lets the lexer carry source text (and, later, line/column info) without forcing the parser to live inside a payload pattern-match. Paren variant names stay long-form (`LeftParen`, `RightParen`) to match Ch 2's enum and avoid the "is it `L` or `Left`?" naming hazard. Next chapters: use `TokenKind.LeftParen` / `TokenKind.RightParen` everywhere. Ch 5's pre-edit text uses the abbreviated `LParen`/`RParen` in `Parser.parse_primary` and `Parser.expect`; the Ch 5 reviser must rename to `LeftParen`/`RightParen`. Ch 7's pre-edit test file uses `TLParen`/`TRParen` (a parallel naming scheme used by a hand-written test); the Ch 7 reviser should consider aligning that too.

## Decision (Ch 4): `.extend()` on slices demoted to 🚧; teach `push` loop / `append` built-in instead
Decided to demote `xs.extend(ys)` to a 🚧 callout in §4.3, even though the spec lists it as the canonical in-place append-all method, because the compiler silently drops the call (verified empirically: `let mut xs = [1,2,3]; xs.extend([4,5,6]); println(xs.len())` prints `3`). Two working alternatives in the chapter: (a) `push` in a loop, (b) re-bind with the `append(xs, x)` built-in. Logged in `cr/docs/book-overhaul-findings.md` and `~/projects/lyric/TODO`. Next chapters: do not show `.extend()` as a current Lyric idiom; if you need in-place append-all, use a `push` loop. When the compiler bug is fixed, this decision can be revisited.

## Decision (Ch 4): UTF-8 framing is a 🚧 roadmap callout in §4.1, not silent
Decided to keep §4.1's UTF-8 status as an explicit italic *🚧 Roadmap: ...* callout rather than the pre-edit's casual "no built-in UTF-8 decoding — if you need Unicode, work with bytes directly" framing. The roadmap callout names the specific operations that are planned (`\u{NNNN}` escapes, `for c in s.chars()`, code-point `char_at`, Unicode-aware case) and reassures the reader the byte-level API stays. The honest "strings are bytes today, code points are 🚧" message is now front-of-chapter, in the same voice as every other 🚧 callout. Next chapters: rely on byte-level string ops freely; don't pretend Unicode methods exist.

## Decision (Ch 4): No `Lexer` class — `tokenize` stays a free function
Decided to skip wrapping the tokenizer in a `Lexer` class. The 70-line `tokenize(input: string) -> [Token]` function has no per-tokenization state worth bundling beyond a local `pos` cursor — making it a class would just add `self.` noise. The class form becomes worthwhile when the lexer needs to carry line/column tracking and produce structured error messages, which is a natural Ch 5 (or later) extension when errors land. Next chapters: if you want a `Lexer` class for source positions, wrap `tokenize` then; Ch 4's free function form is intentional.

## Decision (Ch 5): `Error { msg: ... }` literal is the canonical error-construction form; `new_error(msg)` demoted 🚧
Decided to use the stdlib `Error { msg: "..." }` class literal everywhere in Ch 5 (and going forward) for error construction. The spec lists `new_error(msg): string -> error` in §Built-in Functions §String / Conversion as a one-liner shortcut, and the checker types it correctly (`src/checker/checker.ly:3609`), but the C backend has no lowering for it — `lyric compile` succeeds but the generated C calls `new_error(LYRIC_STR(...))` with no declaration, so gcc fails to link. Verified empirically: `Error { msg: "..." }` compiles + runs end-to-end; `new_error(...)` doesn't. §5.4 introduces the literal form and adds a 🚧 callout about `new_error`. Logged in `~/projects/lyric/TODO`. Next chapters: keep using `Error { msg: ... }`; do not show `new_error(...)` as a current Lyric idiom until the lowering lands. (Carry-forward's prior note that "`new_error(msg)` is the idiom for error values" was based on spec-text only and was empirically false — corrected here.)

## Decision (Ch 5): Stringify errors with `f"{err}"`, not `err.message()`
Decided to teach `f"{err}"` (and `println(f"...: {err}")`) as the way to display an error value of interface type `error`. The natural method-call form `err.message()` fails to compile today: the C backend models `error` as `const char*` (the legacy bare-string representation), so a call `Error_message(e)` against the concrete `Error_message(Error*)` signature is rejected by gcc with "incompatible types". The f-string lowerer has a dedicated path for the `error` type that does the right thing. Verified empirically. Caveat (also logged): the f-string path always pulls the `Error.msg` field directly, so a custom `class ParseError { ... pub func message(self) -> string { f"{line}:{col}: {msg}" } }` will print just its `msg` field through `f"{err}"`, not the formatted "L:C: msg" form — i.e. user-defined `message()` overrides are bypassed by both the call form (compile error) and the f-string form (silently ignored). Both bugs share the same root cause (no real interface dispatch for `error`) and are logged in `~/projects/lyric/TODO`. Next chapters: stringify errors with `f"{err}"`; if you write a custom error class, you can still call `.message()` on it directly (concrete-type dispatch works), but don't promise readers that user-defined `message()` is what gets called when the value is held as `error`.

## Decision (Ch 5): Bare `return` inside `main()` is avoided; use `if/else` instead
Decided to refactor §5.1's `main()` example from early-`return`-on-error to an `if/else` shape because bare `return` inside `main()` fails to compile. The C backend lowers `func main()` as `int main(int _argc, char** _argv)` (to accept the CLI args the runtime injects) but a Lyric `return` with no value lowers to a bare `return;`, which gcc rejects with `'return' with no value, in function returning non-void`. Logged in `~/projects/lyric/TODO`. The fix is on the compiler side — either lower bare `return` in `main` as `return 0;`, or wrap the user's `main` in a void-returning shim. Next chapters: write `main()` examples with `if/else` rather than `if ... { return }` early-exits until the compiler fix lands. `return` from non-main functions is unaffected.

## Decision (Ch 6): `Stack<T>` in §6.9 is illustrative-only with a 🚧 callout; generic *free* functions are taught as the working idiom
Decided to keep the `Stack<T>` example in §6.9 but demote it to "illustrative" with an explicit 🚧 roadmap callout, because generic class methods that access `self.<field>` lower to a null receiver in the C backend today. Verified empirically: `class Stack<T> { items: [T]; pub func push(self, item: T) { self.items.push(item) } }` instantiated as `let mut s = Stack<f64> { items: empty }; s.push(1.0)` compiles through checking, lowering, and monomorphization but the generated C reads `lyric_push(&/* null value */->items, item, LyricSlice_double);` — the receiver pointer is dropped inside the monomorphized method body. Same null-receiver shape for `pop`/`peek`. Generic *free* functions (`max_val<T: Comparable>`, `identity<T>`, `first<T>`, `print_it<T: Printable>`) compile and run end-to-end and are the pedagogical workhorses of the chapter. A secondary finding logged with the same bug: untyped empty slice literal `[]` does not unify with a generic class's type parameter — `Stack<f64> { items: [] }` fails with `validateAllExprsResolved: TypeVar leak 'T'`; the workaround in the book is to bind a typed empty (`let empty: [f64] = []`) first. Both logged in `~/projects/lyric/TODO`. Next chapters: do not show generic-class instance methods with `self.<field>` access as working code until the bug is fixed; generic *free* functions are fully usable today.

## Decision (Ch 6): Soften the Ch 6 opening — Ch 5 does not deliver line/column error reporting
Decided to rewrite Ch 6's first sentence from "Our calculator parses and evaluates expressions, handles errors, and reports line and column numbers" to "Our calculator parses and evaluates expressions, and reports errors when the parser hits something it can't handle." Ch 5 introduces `ParseError { line, col, msg }` as a custom-error *example* in §5.5, but the actual calculator's `Parser` keeps the simple `Error { msg }` form — there's no `Lexer` carrying source positions. Adding a position-tracking `Lexer` was the alternative; that would have pushed Ch 6 past its 30-edit budget and would have shifted the chapter's center of gravity away from generics. Next chapters: the calculator still has no source-position error reporting; if a later chapter needs it, that chapter does the `Lexer` lift.

## Decision (Ch 6): Function-type syntax in inline prose stays `func(T) -> U`
Decided to update §6.2's inline mention of a higher-order signature from `(xs: [T], f: (T) -> U) -> [U]` to `(xs: [T], f: func(T) -> U) -> [U]`. Bare `(T) -> U` for a multi-arg function type isn't grammar (the spec lists `T -> U` for single-arg only and `fn(T,U) -> V` / `func(T,U) -> V` for multi-arg), and `fn(...)` doesn't parse today (Ch 4 finding). `func(...)` is the only form that compiles, so it's the form the book uses. Already consistent with Ch 3's `func(i32) -> i32` precedent.

## Decision (Ch 7): Test the calculator against post-Ch 4 `TokenKind` names; `TNumber` / `TOp` / `TLParen` / `TRParen` are gone
Decided to rewrite every test in §7.2 and §7.7 to use the post-Ch 4 enum names: `TokenKind.Number` (not `TNumber`), `TokenKind.Plus` / `Star` / etc. (not `TOp` — `TOp` never existed; Ch 4's redesign split operators into their own variants), and `TokenKind.LeftParen` / `TokenKind.RightParen` (not `TLParen` / `TRParen`). The pre-edit Ch 7 used a parallel `T*` naming scheme that contradicted Ch 2 / Ch 4 / Ch 5 / Ch 6 — those names had to go. Also rewrote `len(tokens)` → `tokens.len()` to match the book's method-call idiom (carry-forward §Idiomatic Lyric). Verified end-to-end: all 14 test functions compile and execute under `lyric test calculator_test.ly calc.ly`. Next chapters: Ch 13 (modules), if it splits the calculator into files, keeps the same `TokenKind.*` names; Ch 14 (the compiler tests itself) already uses different real-compiler token names (`KLet`, `SEOF`, etc.) and is unaffected.

## Decision (Ch 7): Error inspection is `assert_eq(f"{err}", "...")`, not `err!.message().contains(...)`
Decided to rewrite the pre-edit §7.2 `test_error_message` from `assert(err!.message().contains("unexpected"), ...)` to `assert_eq(f"{err}", "unexpected end of input")`. The `.message()` form is broken on `error`-typed values (Ch 5's interface-dispatch finding — gcc errors `Error_message((const char*)e)`), and `.contains()` on a returned-string was also broken in the same compile (`error_contains` undeclared). The f-string form is the working idiom established in Ch 5 §5.5 and verified end-to-end here against the actual `parse("3 +")` output. The change has the side benefit of being a tighter assertion — `assert_eq` shows expected/got on failure, whereas `assert(...contains)` would just say "false". Next chapters: when a chapter needs to test error messages, use `assert_eq(f"{err}", "...")`. If the spec eventually adds an `Error.contains_message(s)` helper or restores real interface dispatch, this decision can be revisited.

## Decision (Ch 7): `parse()` description is honest about the Ch 5 shape — no fictional `Tokenizer` class
Decided to rewrite §7.2's description of `parse()` from "creates a `Tokenizer`, feeds the result to `Parser`, and returns `(f64, error)`" to "calls `tokenize`, builds a `Parser`, and returns `(f64, error)`." The pre-edit invented a `Tokenizer` *class* that the calculator does not have — Ch 4 (Decision: "No `Lexer` class — `tokenize` stays a free function") explicitly chose a free function, and Ch 5's `parse` matches that shape. The fix is a one-clause edit; the surrounding pedagogy is unchanged. Next chapters: any reference back to the calculator's tokenization layer should call it the `tokenize` free function, not a class.

## Decision (Ch 7): §7.3 "How It Works" drops the unverified `setjmp`/`longjmp` claim
Decided to rewrite §7.3 step 3 from "Generates a C test runner that calls each test function inside a `setjmp`/`longjmp` isolation boundary" to "Generates a C `main` that calls each test function in source order, with result tracking." Inspection of the generated C from `./lyric compile <test.ly> -o /tmp/test.c` shows the runtime header defines `lyric_assert` / `lyric_assert_eq` macros that call `exit(1)` on failure — there is no `setjmp`/`longjmp` and no per-test isolation; a segfault in one test will kill the suite. The chapter now describes what the runner actually does. Same edit also softened the matching "A segfault in one test doesn't kill the suite" claim (which is false today). Next chapters: do not promise crash-isolation between tests. If the runner ever grows real `setjmp` isolation, this can be revisited.

## Decision (Ch 7): Opening example uses idiomatic `"hello".len()`, not free-function `len("hello")`
Decided to rewrite the opening §7.0 `test_string_length` from `assert_eq(len("hello"), 5)` to `assert_eq("hello".len(), 5)` to match the book's idiomatic-Lyric stance (carry-forward §Idiomatic Lyric: "Method-call syntax over free-function for stdlib types where both exist"). Verified end-to-end: both forms compile, the method form is preferred. Next chapters: continue using `.len()` for strings and slices throughout.

## Decision (Ch 7): Chapter written as if `assert` / `assert_eq` work; lowering bug to be fixed overnight
Decided to write the Ch 7 examples and FAIL-output blocks as if `assert(cond, msg)` and `assert_eq(actual, expected, msg?)` route through the runtime `lyric_assert` / `lyric_assert_eq` macros that are already declared in the generated C header. **In reality, the C-backend lowering for both builtins is missing — every call is dropped on the floor and every test silently passes** (verified empirically: `func test_boom() { assert(false, "x") }` compiles to `void test_boom(void) {}`, and `assert(false, "boom")` in `main()` runs past it without crashing). Hewitt (orchestrator) instructed the Ch 7 reviser to write the chapter as if the lowering works because he intends to land the fix before next session. The bug is logged in `~/projects/lyric/TODO` as the most severe finding of the book overhaul. Next chapters: once the lowering lands, every test snippet in Ch 7 — and any test snippets added in Ch 13/Ch 14 that reuse `lyric test` — should be re-verified end-to-end. Until then, `lyric test` reports PASS unconditionally, so passing tests prove nothing about the underlying code.

## Decision (Ch 8): Method-call form for every relation-generated op; free-function form mentioned once as the equivalent expansion
Decided to teach `t.roster_append(p1)` / `r.room_append(g1)` / `add.operands_append(three)` everywhere in Ch 8, and to flag the free-function form (`array_append<Team, Player>(t, p1)`, `dll_append<Room, Guest>(r, g1)`) only as the UFCS-equivalent expansion when the stdlib source snippet is unpacked in §8.2. Rationale: carry-forward §Idiomatic Lyric ("Method-call syntax for relation-generated methods"); Ch 8 is the showcase chapter for relations and is the most important place in the book to model the preferred form. Verified end-to-end against `testdata/arraylist.ly` (rewritten variant), `testdata/ref_list.ly` (already uses `r.room_append`), `testdata/multi_relation.ly` (rewritten variant), `testdata/destroy_shared.ly` (rewritten variant), and a fresh AST snippet matching §8.6 — all four compile and produce the chapter's documented output. Next chapters: continue method-call form for any relation use. Ch 9's `MyList` example already uses the method form (`panel.add(w1)` / `panel.count()`) — keep that as-is.

## Decision (Ch 8): §8.2 stdlib excerpt mirrors the real `ArrayListBase` + `ArrayList` split, not a monolithic `ArrayList`
Decided to rewrite the §8.2 "here's the actual source" snippet to match what `stdlib/std.ly` actually does today: an `ArrayListBase<P, C>` interface that declares the fields and the `P.append` / `P.remove` methods (plus the free-function `array_append` / `array_remove`), and an `ArrayList<P, C>` interface that `embed`s `ArrayListBase` and adds the cascade destructors. The pre-edit snippet showed a single monolithic `ArrayList` interface containing both ops and destructors, and used getter/setter form (`parent.children()`, `child.set_parent(parent)`) for the bodies — neither matches the current stdlib. The refreshed snippet uses direct field access (`self.children`, `child.parent = self`) and shows the `trusted` keyword on the methods (which is how the stdlib actually opens the `ref child` / `unref child` calls that maintain back-pointer ref counts). Bonus pedagogy: the `embed` keyword gets a working example *before* Ch 9 has to introduce it formally — and Ch 8 explicitly forward-points to a sibling `RefArrayList` that embeds the same base but uses non-cascading destructors. Next chapters: Ch 9 §9.5's `ArrayList` destructor snippet should be aligned to the same direct-field-access style (logged in Ch 8 inheritance note above).

## Decision (Ch 8): New §8.7 teaches `final func`; old §8.7 renumbered to §8.8
Decided to add a short new subsection §8.7 introducing `final func` as the user-code-at-destruction-time escape hatch (file handles, network connections, external registries) — sandwiched between the AST cascade-destruction climax (§8.6) and the "Trade-Off" wrap-up (now §8.8). Rationale: `final func` is mentioned exactly once in the pre-edit book (Ch 11 line 3469, in passing — "any `final func` cleanup you declared") and never explained. Ch 8 is the natural home: it's where destruction is introduced, and `final` fits the chapter's central pattern (Lyric infers what it can, and exposes a named escape hatch for what it can't). Spec §Class Destruction documents it. Verified end-to-end: the `Connection { name: string; final func cleanup(self) { ... } }` snippet from §8.7 compiles and runs, printing `closing db` on scope exit. A 🚧 callout in §8.7 notes the double-fire-on-explicit-`destroy()` bug logged in `~/projects/lyric/TODO`. Next chapters: Ch 11's mention of `final func` can stay as a brief reference now that Ch 8 has done the teaching.

## Decision (Ch 8): §8.3 output shows g2 surviving with name "Bob", matching real `testdata/ref_list.ly` behavior
Decided to fix the §8.3 output block to include `g2 name: Bob` alongside `g1 name: Alice` and `g3 name: Carol`. The pre-edit version showed only g1 and g3 in the post-destroy output, implying that the explicitly-removed `g2` was somehow gone — but `r.room_remove(g2)` is a `refs` operation: it just unlinks, it doesn't destroy. `g2` is alive until its local binding goes out of scope. The chapter prose is also tightened to call this out explicitly ("Bob is doubly interesting: we explicitly removed him from the room… and he's still alive at the end because nobody owns him"). Matches the verified output of `testdata/ref_list.ly`. Reinforces the chapter's core distinction (`owns` cascade-destroys, `refs` only unlinks) by showing both an explicit `r.room_remove(g2)` *and* a parent `r.destroy()` and observing that neither destroys the guests.

## Decision (Ch 9): §9.3 rewritten to use the working default-method shape — top-level free-function call + getter/setter inside
Decided to rewrite §9.3 ("Method Syntax on Interfaces" → "Default Methods and Field Accessors") because the pre-edit example didn't compile. The pre-edit shape — `pub func P.add(self, child: C) { let kids = self.items(); ...; self.set_items(append(kids, child)) }` inside `interface MyList<P, C>`, called as `panel.add(w1)` after `relation MyList Panel:w owns [Widget:p]` — fails at three points: (a) `panel.add(w1)` → `checker: unknown method: add`; (b) `panel.w_add(w1)` (the label-prefixed form spec §Default Methods Are Label-Prefixed promises) also fails the same way; (c) the body's references to `self` lose their type after the receiver-binding declaration. Only the stdlib hints (`ArrayList`/`OwningList`/`RefList`/`HashedList`) currently get label-prefixed methods on the parent — user-defined hints don't. The post-edit example uses the only end-to-end-compilable shape: top-level default function `pub func add(parent: P, child: C)` declared inside the interface, getter/setter form for field access (`parent.items()` / `parent.set_items(...)`), and free-function call with explicit type args at the call site (`add<Panel, Widget>(panel, w1)`). A 🚧 callout points readers at the stdlib relation methods (`team.roster_append(p)`, `dir.files_append(f)` from Ch 8) when they want method-call ergonomics today. Both bugs (default-method method-syntax + user-defined-hint label-prefixed methods) logged in `~/projects/lyric/TODO`. Next chapters: when an interface needs ergonomic method-call syntax for the user, lean on the four stdlib hints or accept the free-function call form; don't promise `panel.add(...)` / `panel.w_add(...)` for user-defined interfaces until the desugar pass is generalized.

## Decision (Ch 9): §9.2 adds a roadmap callout that default-method method-syntax (`g.count_edges()`) is checker-rejected today
Decided to add a one-paragraph 🚧 *Roadmap* callout to §9.2 after the `count_edges<...>(g)` example, explaining that the spec promises method-call syntax for default methods (`g.count_edges()`) but the checker only resolves the free-function-with-explicit-type-args form. Verified empirically against an extract of `testdata/interfaces.ly` with the call site rewritten. The chapter keeps the free-function form (which is what the testdata uses and what compiles); the callout sets the expectation honestly. Logged in `~/projects/lyric/TODO`. Next chapters: assume free-function form for default-method invocations until the lift lands.

## Decision (Ch 9): §9.5 ArrayList destructor snippet uses direct-field-access style (per Ch 8 reviser's flag)
Decided to align §9.5's `ArrayList<P, C>` destructor snippet to the direct-field-access form the Ch 8 reviser established in §8.2: `self.children[i].destroy()` and `self.children.len() - 1` instead of `parent.children()` and `len(children)`. Matches the real `stdlib/std.ly` `ArrayList<P, C>` (which is built on `ArrayListBase<P, C>` and uses direct field access throughout, with `trusted` for the `ref` / `unref` calls). The `destructor C` body still calls `array_remove(self)` because that's the stdlib's own form. Pedagogical consistency with Ch 8 §8.2 — Ch 9 is now the place where readers can see the same shape from two angles (Ch 8 from the user's side, Ch 9 from the interface's side). Next chapters: when a destructor body operates on concrete-typed fields (after interface fields have been injected into the class), use direct field access; the getter/setter form is reserved for interface default-method bodies where the receiver is still a type variable.

## Decision (Ch 9): §9.1 and §9.7 modernized to `xs.len()` method form (was `len(xs)` free-function form)
Decided to rewrite `len(nodes)` / `len(edges)` / `len(kids)` to `nodes.len()` / `edges.len() as i32` / `kids.len() as i32` in §9.1's `count_edges` default method and §9.7's `count_children` generic function — both to match carry-forward §Idiomatic Lyric ("Method-call syntax over free-function for stdlib types where both exist") and to match what `testdata/interfaces.ly` uses. The `as i32` cast appears because `len()` returns `i64` and the surrounding arithmetic / return type is `i32`; the free-function `len(...)` form returned `i32` directly. Verified end-to-end: both rewritten snippets compile and produce the same output as the testdata. Next chapters: continue `.len()` everywhere; only fall back to `len(x)` if the spec ever marks `.len()` as 🚧 on a specific type, which it currently doesn't.

## Decision (Ch 9): §9.10 stdlib line counts refreshed (733 / 991, was 617 / 875)
Decided to update §9.10's "617 lines of Lyric in `std.ly` alone (875 including `string.ly`)" claim to "733 lines of Lyric in `std.ly` alone (991 including `string.ly`)" per a fresh `wc -l stdlib/*.ly`. The stdlib grew between when §9.10 was written and now (the `ArrayListBase` / `ArrayList` / `RefArrayList` factoring the Ch 8 reviser landed accounts for some of the growth; the rest is the `DoublyLinked` method-form additions visible at `stdlib/std.ly:168-200`). Numbers are load-bearing for the "the stdlib is the proof" closing argument, so they need to be real. Next chapters: if any chapter cites stdlib LOC, re-`wc` at revision time.

## Decision (Ch 10): §10.5 rewritten as a standalone "Variable Bindings" example (no calculator graft)
Decided to rewrite §10.5 from "A Symbol Table for the Calculator" (a `class Calculator { vars: Dict<Sym, f64> }` + a fictional `parse_primary` hook checking `TokenKind.Ident`) to "A Symbol Table for Variable Bindings" (a top-level `let vars = Dict<Sym, f64>()` plus a free function `get_var(d: Dict<Sym, f64>, name: string) -> (f64, error)`). Two independent reasons: (1) the task prompt is explicit that "Calculator through-line is complete by Ch 8. Ch 10+ uses standalone examples" — §10.5 was the one place that rule was being violated; (2) the pre-edit code didn't compile anyway — `Dict<Sym, f64>` as a field on a non-generic class leaks `V` at the checker (`validateAllExprsResolved: TypeVar leak 'V'`), and the pre-edit's `parse_primary` snippet referenced `TokenKind.Ident` and `self.calc` neither of which exist in the Ch 5 calculator. The rewritten §10.5 compiles + runs end-to-end (output `pi = 3.14159` / `undefined variable: tau`); a 🚧 callout names the Dict-as-field limitation honestly and points readers at the working top-level shape. Both compiler bugs (Dict-as-field typevar leak + generic-outer monomorphizer failure) logged in `~/projects/lyric/TODO`. Next chapters: when teaching `Dict` use, keep it as a local / parameter, not as a field of another class, until the typevar-leak and monomorphizer bugs are fixed.

## Decision (Ch 10): String-keyed Dict literals demoted to 🚧; backtick-sym and integer-key forms taught as the working syntax
Decided to remove the spec-promised string-keyed Dict-literal example (`let cities = {"NYC": 8_000_000, "SF": 875_000}`) from §10.3 and replace it with a 🚧 roadmap callout pointing readers at `let d = Dict<string, V>()` + explicit `.set(...)` calls. Empirical: the parser rejects `{"<string>":` openings with `expected PRParen, got SEOF ()` — the dict-literal disambiguator commits to a struct-literal interpretation when the first token after `{` is a string-literal and trips at the closing brace. Backtick-sym keyed (`{`alice`: 1, `bob`: 2}`) and integer-keyed (`{1: "one", 2: "two"}`) both parse, compile, and run end-to-end against the stdlib `Dict` — those stay in the chapter as the two working forms. Logged in `~/projects/lyric/TODO`. Next chapters: if a code example needs a string-keyed map, build it with the `Dict<string, V>()` constructor and explicit `.set(...)` calls rather than a literal; reach for backtick-sym keys whenever the keys are identifier-shaped (which is most of the time in compiler/interpreter code).

## Decision (Ch 10): `Hashable` honestly framed as single-method; `Dict.length()` vs spec's `Dict.len()` sidestepped
Decided to (a) document `Hashable` in §10.2 as the single-method interface it actually is today (`func get_hash(self) -> u64`) with a 🚧 callout in §10.1 and §10.3 about the missing `equals` method, and (b) never write `d.length()` or `d.len()` in any §10 example — instead, when the chapter needs to talk about Dict size, it pulls `let keys = d.keys()` and writes `keys.len()` (which works because slices have `.len()`). Rationale: stdlib has `Dict.length()` while spec §Dict<K,V> table lists `len()`; both are 🚧 candidates for renaming (the carry-forward §Idiomatic Lyric stance is `.len()` everywhere). Sidestepping keeps the chapter correct regardless of which fix lands. The `HashedList`-matches-by-hash-alone callout (also flagged in carry-forward §Roadmap §Stdlib) is the load-bearing honest framing — for `Sym` keys the chapter's examples are collision-safe, for non-`Sym` keys they're not, and the chapter says so twice. Logged in `~/projects/lyric/TODO`. Next chapters: keep using `xs.len()` for slices; if Ch 11+ needs to query a Dict's size, prefer iterating over `d.keys()` or wait until the `Dict.len` / `Dict.length` rename lands.

## Decision (Ch 11): The three lifetime regimes get their own subsection (§11.5); refcounting is current Lyric per the spec
Decided to add a new §11.5 "The Three Lifetime Regimes" that names Owned / Permanent / Refcounted explicitly. The pre-edit chapter explained `owns`-driven cascade destruction (§11.4) and slice-level escape analysis (old §11.5, now §11.6) but never named the three regimes the spec §Memory Model formalizes, and never gave `permanent` more than a passing mention in the closing paragraph of §11.4 — even though Ch 10 made `permanent` load-bearing for `SymTable`. The task brief flagged ref counting as 🚧 status, but both the spec §Memory Model ("3. Refcounted — all other classes; the compiler inserts StRefIncr/StRefDecr ops") and the language reference doc ("Owned classes destroyed via their owning relation; unowned classes are refcounted") present it as a current regime, with `--rc-free` (default ON) wiring RC=0 to `destroy()` and `--no-rc` as the explicit opt-out. I trusted the spec over the brief and presented refcounting as current with no 🚧. The "move semantics are inferred" paragraph moved from the §11.7 (formerly §11.6) Copy-on-Assign tail into §11.5 so the entire lifetime-management story lives in one place. Next chapters: when discussing class lifetime, refer to one of the three named regimes; `permanent` is the established opt-out for whole-program-lifetime singletons (Ch 10 `SymTable` is the canonical case).

## Decision (Ch 11): §11.4 destroy-shared example converted to method-call form; §11.8 inventory rewritten to remove non-existent Calculator class
Decided two surgical fixes in service of the Ch 8 §Idiomatic Lyric mandate and the Ch 10 carry-forward note. (a) In §11.4, converted `dll_append<TeamA, Player>(a, p)` / `dll_append<TeamB, Player>(b, p)` to the method form `a.team_a_append(p)` / `b.team_b_append(p)`. The example mirrors `testdata/destroy_shared.ly`, which uses the free-function form, but Ch 8's "method-call form for every relation-generated op" decision applies; verified end-to-end (`./lyric compile` + gcc + run) — identical output to the test file. (b) In §11.8 (formerly §11.7), rewrote the "What the Calculator Costs" inventory. The pre-edit listed `Calculator` and `Dict` as fields of a calculator class with a cascade-via-`owns` relation between them — but Ch 10's reviser explicitly rewrote §10.5 to a *standalone* `Dict<Sym, f64>` variable-bindings example precisely because the `Calculator { vars: Dict<Sym, f64> }` shape doesn't compile today (TypeVar leak; see Ch 10 findings). The new §11.8 inventories what the book actually built: tokens as stack structs, the token slice as scope-exit-freed, the `Parser` as refcounted, and the §10.5 `Dict`/`DictEntry`/`Sym` as an additive bolt-on with honest framing of `Dict`'s `owns` relation to its entries and `SymTable`'s `permanent` status. Next chapters: don't reference a `Calculator` class — it doesn't exist in the book. The closing artifact is `tokenize` → `Parser` → `parse`, plus a standalone `Dict<Sym, f64>` example.


## Decision (Ch 12): Lowercase `lock` is the only spelling; capital `Lock` / `Mutex` honestly framed as removed (not "synonyms slated for removal")
Decided to rewrite the §12.5 parenthetical "(Older code, or code translated from Rust, may use `Mutex` or `Lock` — both are lowerer-level synonyms that are slated for removal. Lowercase `lock` is canonical.)" to an honest one-line note: lowercase `lock` is the only spelling that compiles today; capital `Lock` produces `post-mono: main mu has unresolved type var 'Lock'` and fails downstream. Verified empirically — `/tmp/lock_lower.ly` compiles + runs (`final count: 11`), `/tmp/lock_upper.ly` errors at mono. Matches carry-forward §Permanent Invariants ("Lowercase `lock` is canonical (NOT `Mutex` / capital `Lock` — both removed)") and spec §Recently Removed. Note: `testdata/lock.ly` still has `let mut mu: Lock` — that testdata file is stale and should be updated, but it's out of scope for the book overhaul. Logged here as a follow-up. Next chapters: never spell the mutex type any way other than lowercase `lock`.

## Decision (Ch 12): §12.3 producer example fixed — start from 1, terminate on `0` sentinel
Decided to rewrite the §12.3 producer from `i = 0; while i < count; send(i)` (producing `0, 1, 2, 3, 4`) to `i = 1; while i <= count; send(i)` (producing `1, 2, 3, 4, 5`) and the consumer loop condition from `while val >= 0` to `while val > 0`. The pre-edit example **did not terminate** — verified empirically by running it under `timeout 3 /tmp/producer_bin` and watching it spin forever printing `got: 0`. The bug: `receive()` on a closed channel returns the zero value (`0` for `i32`), and the consumer's `val >= 0` condition is *always* true for the zero return — *and* the producer's first real send is also `0`, so the prose's own claim ("zero-value 0 on close terminates the loop only because the producer happens to send positive values first") was doubly wrong. The fix preserves the chapter's pedagogical point about the missing `(value, ok)` tuple — and is honest about *why* we start from `1`: we need a value the producer will never send to use as a sentinel. The §12.3 🚧 callout was rewritten to give three explicit workarounds for the no-tuple-on-close gap (sentinel value, separate `done` channel, `Optional` wrapping) with a note that option 2 is most robust when you don't control the producer's value range. Next chapters: when teaching channel close-detection, either commit to a domain-safe sentinel (and document it), use a separate `done` channel, or wait for the `(T, bool)` form to land.

## Decision (Ch 12): §12.4 `select` polls with `usleep(100)`, not `sched_yield()` — fixed to match spec
Decided to fix §12.4's claim that `select` compiles to "try each case, run `default` if present, otherwise `sched_yield()` and retry" — spec §Concurrency line ~1696 explicitly says `usleep(100)` (and the 🚧 in the spec is specifically about the wasteful sleep-based polling, not yielding). The corrected 🚧 callout now names the ~100µs latency cost honestly. The chapter's underlying point (select is not a true blocking primitive today, condvar/epoll is the roadmap target) is preserved. Next chapters: when discussing `select` mechanics, the implementation detail is `usleep(100)` polling; don't say `sched_yield`.

## Decision (Ch 12): §12.1 `spawn`-by-pointer-race callout consolidated and strengthened
Decided to merge the pre-edit §12.1's two separate "captures by pointer" paragraphs (a 🚧 callout *and* a closing prose paragraph that repeated the same point in slightly weaker language) into a single, more substantive 🚧 callout. The new callout explicitly distinguishes the safe case (channels — class pointers with internal locking) from the unsafe case (a captured `let mut counter: i32 = 0` mutated by two `spawn` blocks is a textbook race), names three workarounds in order of preference (copy-in inside the spawned block; `lock` from §12.5; funnel writes through a channel to a single owner), and reiterates the roadmap intent. This is the chapter's most important honesty point — the data race is real, silent, and easy to hit — so it gets the consolidated treatment rather than two scattered hedges. Next chapters: when forward-referencing `spawn`-capture from elsewhere in the book, point at §12.1 and the by-pointer-race callout, not at any other paragraph.

## Decision (Ch 12): §12.7 intro corrected — workers compute partial sums, not "increment a shared counter"
Decided to fix the §12.7 intro sentence "two spawned workers increment a shared counter through a channel" to "two spawned workers each compute a partial sum and ship it back through a channel." The example has no shared counter; each worker has a local `mut sum: i32` and sends the final value once. The pre-edit framing made it sound like the example demonstrated *shared mutable state through a channel*, which is the opposite of what it actually shows (the chapter's closing point is "share memory by communicating, not by locking"). The fix is one sentence; the closing paragraph and the example itself are unchanged. Verified end-to-end: `total: 21` (= 1+2+3+4+5+6).

### After Ch 13
- The calculator is conceptually re-split in Ch 13 §13.5 as a teaching example: `lexer.ly` + `parser.ly` + `main.ly` all in package `main`, sharing one namespace via flat-file merge (no `import` between them). The actual `calc.ly` source the book has been building is *not* modified — §13.5 shows the structural split with elided bodies (`// ... parse_expr / parse_term / parse_primary, exactly as in §5.6 ...`) because the chapter's topic is package mechanics, not parser rewrites. The Ch 5 `tokenize` / `Parser` / `parse` shapes are referenced but not duplicated.
- **Types introduced (carrying forward):** *none*. Every Ch 13 type is illustrative (`mylib.Point`, the `Token`/`TokenKind` reprises, the cross-package `lexer.Token` with `make_number` constructor) and doesn't carry into Ch 14.
- **Language features introduced and load-bearing for later chapters:** `lyric <name> { }` package-block syntax (optional wrapper; package name comes from the directory regardless); `pub` visibility across packages (today: declared, not yet enforced); `import name`, `import alias from "path"` (the two working import forms); `lyric.mod` as a module-root marker (existence-only check today, content not parsed); auto-import of `stdlib/std.ly` + `stdlib/string.ly` (991 lines combined); `lyric compile <dir>` directory mode (auto-detected when `lyric.mod` is present); flat-file-list mode (`lyric compile a.ly b.ly c.ly`) as the workaround when imports can't carry the structure; `.lyric` declaration files and the **lyre** sibling toolchain (CDD annotations live there, not in `.ly` source).
- **Idiomatic patterns established in Ch 13:** `lyric compile <dir>` (with a `lyric.mod`) is the canonical compile command for multi-file projects; flat-file-list is the canonical scale-up pattern when imports run out of road; constructor functions (`pub func new_point(...) -> Point`) are the working cross-package data-passing idiom because qualified type names don't resolve; `pub` on everything you intend to export (write it now, the filter is on the roadmap).
- **What §13.5 verified end-to-end (`./lyric compile` + gcc + run):**
  - The §13.1 two-package `mylib_demo` example (matches `testdata/import_dir` byte-for-byte): output `3`.
  - The §13.5 multi-file single-package shape (synthetic `lexer.ly` + `parser.ly` + `main.ly` with `tokenize` + `parse_simple` + `main`): compiles, runs, prints `= 2` (the simplified version counting Number tokens). The §13.5 prose elides the Ch 5 parser body for space; the structural shape (`p.parse_expr()` call from `parser.ly` referencing `Parser` from same file, `parse(...)` call from `main.ly`) compiles cleanly.
  - The §13.5 cross-package addendum with `make_number` constructor (`/tmp/calc_xpkg`): compiles, runs, prints `2 tokens`.
  - The §13.6 aliased import `import ln from "longname"`: compiles, runs.
- **Numbers refreshed in Ch 13:** Lyric LOC 30,796 → **33,500** across 14 files in 12 directories; C LOC 105,457/105,458 → **114,770**; stdlib 617/875 → **733/991**. All from a fresh `wc -l` against current source. If Ch 14 (or §13.9) cites any of these, use the same numbers.
- **Known gaps deliberately left for later / 🚧 callouts added in Ch 13:**
  - (1) **§13.5** — *Qualified type and enum-variant resolution.* `let xs: [lexer.Token] = []`, `lexer.Token { ... }`, and `lexer.TokenKind.Plus` all fail with `unknown struct/class: lexer.Token` even though `lexer.tokenize(...)` resolves cleanly. The book teaches the constructor-function workaround (`pub func new_point(...) -> Point` + inferred receiver typing) and explicitly demotes qualified-type usage to 🚧. This is the **single biggest** module-system gap today — it's why the Lyric compiler itself uses zero `import` statements. Logged in `~/projects/lyric/TODO` and findings.
  - (2) **§13.5** — *Recursive imports.* Only the root file's `import` statements are processed; `parser/parser.ly` saying `import ast` is silently ignored. Combined with (1), package hierarchies can't go more than one level deep with `import`. Already in spec §Imports as 🚧.
  - (3) **§13.6** — *Bare `import "path"` is a known crasher.* Parses but the resolver has no codepath to derive an alias from the path basename. Use `import alias from "path"` instead. Already in spec §Imports as 🚧.
  - (4) **§13.7** — *`pub` is not filtered across imports.* Declared but not enforced; every declaration in an imported package is visible after prefixing. Write `pub` anyway so code is correct when the filter lands. Already in spec §Visibility as 🚧.
  - (5) **§13.7** — *No cycle detection.* Moot under single-level rule; load-bearing once recursive imports land.
  - (6) **§13.7** — *No re-exports.* If `parser` imports `ast`, callers who need `ast.something` import `ast` themselves.
  - (7) **§13.7** — *No package registry.* `lyric get` / `lyric add` don't exist; copy third-party code into your tree.
- **Spec/CLI/book drift caught and fixed:**
  - `lyric compile -mod .` (pre-edit §13.3) does not exist — there is no `-mod` flag; directory mode is automatic (`src/main/main.ly:735–765`). §13.3 rewritten to teach `lyric compile .`.
  - §13.5 pre-edit used `LParen`/`RParen`; replaced with `LeftParen`/`RightParen` per Ch 4 decision (since the example reprises the calculator's `TokenKind`).
  - §13.5 pre-edit used `match`-as-expression with bare-identifier arms (`'+' => ast.TokenKind.Plus`); the parser rejects this. Rewrite uses an `if`/`else if` chain on `let mut kind`. (Match-as-statement with braced bodies works; match-as-expression with bare-value arms doesn't parse today — also a Ch 6 reviser finding worth noting.)
  - §13.8 (pre-edit "The Compiler as Example") claimed `checker imports ast and lexer` and `main imports everything`. Empirical: the entire compiler source has **zero `import` statements** — verified with `grep '^import' src/*/*.ly`, which returns nothing. The compiler uses flat-namespace merging of `BOOTSTRAP_FILES`. Section renumbered §13.9 and rewritten to teach this honestly, with the Makefile excerpt as the proof.
  - §13.2 pre-edit said `105,457-line C file`; refreshed to `114,770`.
  - §13.4 pre-edit said `617 / 875` stdlib lines; refreshed to `733 / 991`.
- **New §13.8 ".lyric Sibling" added** to satisfy the carry-forward's "lyre framing required in Ch 13" criterion. Brief — points at the preface's "A sibling artifact: lyre" subsection for the full pitch. Establishes that `.lyric` files are declaration-only Lyric, lyre is a separate toolchain (Go/Python/TypeScript/Lyric), and CDD annotations (`why:`, `doc`, `invariant:`, `source:`, `fake:`) are **lyre features, not Lyric features** — so they never appear in `.ly` files or in the rest of this book.
- **Section reorganization:** Ch 13 now has nine sections (was eight). New §13.1 "Packages, Imports, and `pub`" heading on what was the unheaded opener; new §13.8 ".lyric Sibling"; old §13.8 "The Compiler as Example" renumbered to §13.9. Subheader and subnumbering elsewhere unchanged.
- **Ch 14 inheritance:** Ch 14 §14.6's numbers table cites `c_backend.ly` at 5,302 lines and the total at 29,921 — those are stale; current `wc -l` gives `c_backend.ly` 5,551 and total 33,500. Ch 14 reviser should re-`wc` at revision time. Ch 14 §14.0 also says "29,921 lines of Lyric to 105,458 lines of C" — both numbers stale (use 33,500 / 114,770). Ch 14 §14.5 calls `lyric.c` "105,458 lines"; same fix needed there. Ch 13 does **not** modify the calculator's runtime code; nothing in Ch 14's pipeline tour needs to change because of Ch 13.

## Decision (Ch 13): `lyric compile <dir>` is the canonical command; the `-mod` flag pre-edit was fiction
Decided to rewrite §13.3's "`lyric compile -mod .`" to "`lyric compile .`" because the `-mod` flag does not exist. The compiler auto-detects module mode: if you pass a directory containing `lyric.mod` it walks that directory's top-level `.ly` files and resolves the root file's imports; if you pass a single `.ly` whose parent contains `lyric.mod` it uses that parent as the module root. Verified by reading `src/main/main.ly:735–765` and end-to-end against `testdata/import_dir`. Next chapters: any compile-command shown in code blocks should use `lyric compile <dir>` (with `lyric.mod`) for module mode, or `lyric compile a.ly b.ly ...` (no module mode) for the flat-file-list mode the compiler itself uses.

## Decision (Ch 13): Multi-file single-package is the headline split pattern; cross-package is the addendum
Decided to teach §13.5 calculator-splitting as **three files all in package `main`** (one namespace, no `import`), with cross-package splitting framed as a secondary pattern with sharp edges (qualified-type-name limitation). Rationale: (a) the multi-file single-package pattern works unconditionally and produces ergonomic code (no `mylib.` prefixes everywhere); (b) it's the exact pattern the real Lyric compiler uses across 14 files; (c) cross-package today is genuinely limited — `let xs: [lexer.Token] = []` doesn't compile, only function-call references resolve. Teaching cross-package first would set up readers for failure; teaching it second, with explicit 🚧 callouts on the qualified-type limitation, sets the right expectation. Next chapters: when a chapter wants to split example code across files, prefer multi-file single-package by default; reach for `import` only when you genuinely want a hard package boundary (and accept the constructor-function pattern).

## Decision (Ch 13): The Lyric compiler's "no `import` statements" is a feature of the pedagogy, not a flaw to hide
Decided to be honest in §13.9: the compiler is 33,500 lines across 14 files in 12 directories, and there is not one `import` statement among them. Pre-edit §13.8 claimed "`parser` imports `ast`", "`checker` imports `ast` and `lexer`", "`main` imports everything" — all false. The compiler relies entirely on flat-namespace merging of `BOOTSTRAP_FILES`. Rather than hide this (which would require fabricating imports that aren't there), the chapter teaches it as a deliberate response to today's qualified-type-name limitation: when the import system can't carry your types, treat directories as visual structure and let the merge pass make one namespace. The Makefile excerpt shows the exact pattern. Next chapters: Ch 14 (the compiler tour) can lean on this — the pipeline crosses "package boundaries" but those boundaries are organizational, not enforced.

## Decision (Ch 13): `.lyric` mode + lyre get their own short subsection (§13.8)
Decided to add a one-paragraph §13.8 establishing that `.lyric` files are declaration-only Lyric, lyre is the separate toolchain that reads them (Go/Python/TypeScript/Lyric implementations), and CDD annotations (`why:`, `doc`, `invariant:`, `source:`, `fake:`) are **lyre features, not Lyric features**. Rationale: the carry-forward §Permanent Invariants explicitly required this framing in Ch 13 if it wasn't already present, and the pre-edit had no mention of `.lyric` mode anywhere. The subsection is deliberately brief — the preface already pitches lyre via its "A sibling artifact: lyre" subsection; Ch 13's job is to make sure readers who skim know `.ly` and `.lyric` are different and that CDD annotations live in the latter, so they don't try to write `why:` in a `.ly` file. Next chapters: do not document CDD annotations as Lyric syntax. If a chapter wants to reference lyre, point at the preface and §13.8.

### After Ch 14
- **Book complete.** Ch 14 is the closer — no further calculator state to carry; no further chapters to hand off to. Appendix A onward is reference material, not narrative.
- **No types or methods introduced.** Ch 14 is a tour of the existing compiler source (`src/*.ly`); it documents the pipeline but adds nothing new to the running language surface.
- **Numbers refreshed against current source (`wc -l` at edit time):** `c_backend.ly` 5,551, `checker.ly` 4,919, `monomorphizer.ly` 3,939, `lowerer.ly` 3,669, `memory.ly` 2,855, `main.ly` 1,602, `optimizer.ly` 1,556, `desugar.ly` 1,534, `expr_parser.ly` 1,496, `ast.ly` 1,476, `parser.ly` 1,383, `lir.ly` 943, `modules.ly` 907, `lexer.ly` 679. **Compiler total: 32,509.** Stdlib: `std.ly` 733 + `string.ly` 258 = **991.** **Grand total: 33,500.** C output `lyric.c`: **114,770 lines.** Test files under `testdata/`: 89 `.ly` files, 83 with golden output under `testdata/golden/`. These match the Ch 13 narrative figure of 33,500 (compiler + stdlib) exactly.
- **§14.4 ranking corrected:** `c_backend.ly` was called "the second largest file" pre-edit; with current line counts it is THE largest file in the compiler (5,551), narrowly beating `checker.ly` (4,919). Fixed.
- **§14.5 rewritten end-to-end.** Pre-edit framed the bootstrap as Go-compiler-driven (Stage 1 = Go compiles `src/*.ly` → bootstrap1.c, etc.), which is two compiler generations out of date. New §14.5 leads with the *current* fixed-point property: `./lyric` compiles itself → C; GCC compiles that C → new `./lyric`; new `./lyric` compiles itself → same C. The Makefile's `BOOTSTRAP_FILES` list is shown explicitly (matches Ch 13 §13.9's framing), `make update` is named as the rebuild loop, and `bash test_self_compile.sh` is named as the fixed-point regression check (Stage 0 = build from checked-in `lyric.c`, Stage 1 = self-compile to `stage2.c`, Stage 2 = stage2 binary compiles `src/` → `stage3.c`, Verify = `diff stage2.c stage3.c` empty). The Go-compiler origin is preserved as a one-paragraph history note at the end of the section ("the Go compiler is retired; the checked-in `lyric.c` replaced it"). This is the chapter's load-bearing pedagogical moment — the "magic of bootstrap" the prompt called out.
- **§14.7 "Modules" bullet corrected.** Pre-edit claimed "Modules organizing the 12 packages" which contradicts Ch 13's honest framing. Replaced with: "Multi-file packages — 14 `.ly` files across 12 directories, each directory a `lyric <name> { }` block, all flat-merged via `BOOTSTRAP_FILES` on the command line (no `import` statements — see §13.9)." Now matches Ch 13's "the compiler has zero `import` statements" finding.
- **Closing beat added.** Ch 14 (and the book) now ends on a one-paragraph outro tying the chapter back to the reader's journey: "the Lyric you've been learning across these fourteen chapters is the same Lyric that compiles itself, at a fixed point, in a few seconds." Lands the bootstrap-as-payoff theme that §14.5 set up.
- **No new TODOs filed.** Every fix in Ch 14 was an editorial refresh against current source; no compiler bugs surfaced during this revision pass.
- **Spec drift noted but not blocking:** `lyric-language-spec.md:25–27` still says "~30K lines of Lyric → ~105K lines of C" as the self-host description. Now stale (33,500 / 114,770). Worth a spec-side refresh in a future pass; not in this chapter's edit scope.

## Decision (Ch 14): The bootstrap section leads with the *current* fixed point, not the Go origin
Decided to rewrite §14.5 so the bootstrap story foregrounds today's reality — `./lyric` compiles `src/` → `lyric.c`, GCC builds the binary, the binary compiles `src/` again to the same `lyric.c`, `test_self_compile.sh` verifies the diff is empty — rather than the pre-edit's three-stage Go-compiler narrative (which described a build process retired before the book shipped). The Go origin is preserved as a single closing "Origin" paragraph for historical context, but the load-bearing pedagogy is the current self-hosted fixed point. Rationale: (a) the prompt explicitly asked for "the magic of bootstrap (the compiler compiling itself), and probably show the fixed-point property"; (b) the Go-compiler stages don't reflect any command anyone can actually run today, while `make` / `make update` / `bash test_self_compile.sh` are the live commands; (c) the new framing dovetails with Ch 13 §13.9's `BOOTSTRAP_FILES` story so the two chapters reinforce each other. Next chapters: none — Ch 14 is the closer.

## Decision (Ch 14): Compiler + stdlib totals stated as "32,509 + 991 = 33,500" to reconcile with Ch 13
Decided to publish the line-count table with the empirically measured per-file numbers (summing to a compiler total of 32,509), then add stdlib (991) for a grand total of 33,500, which matches Ch 13's narrative number. Rationale: the Ch 13 reviser shipped "33,500 lines across 14 files" without breaking it down; my fresh `wc -l` against `src/*.ly` gives 32,509, and adding the auto-imported stdlib (`std.ly` 733 + `string.ly` 258 = 991) reaches 33,500 exactly. So Ch 13's number is correct as "compiler + stdlib", and Ch 14's table makes the decomposition explicit. No edit to Ch 13 needed; the two chapters are now consistent under the same headline figure. Next chapters: none — Ch 14 is the closer.

### After App A
- **Calculator state unchanged.** Appendix A is reference material — no calculator code, no narrative; just a syntax + builtins quick-ref. The Ch 5 `Parser`, `Token`, `TokenKind` and the §10.5 variable-bindings example are not touched and not referenced.
- **Sweep against §Recently Removed:** `cascade` keyword was already correctly demoted to 🚧-reserved in the Control-flow keyword table (no edit needed there). However, the §Declarations code block had a stale `interface MyList<P, C> { ... destructor P { cascade P.children { C.destroy(self) } } }` line — `cascade { body }` as a statement is removed and the body would no-op today. Rewrote the destructor to use a `for child in self.children { child.destroy() }` loop, which is what Ch 8's relation destructors actually do.
- **Sweep against §Idiomatic Lyric / current grammar:**
  - **§Type construction code block** used `;` as the struct field separator and as the enum-variant separator (`struct Point { x: f64; y: f64 }`, `enum Color { Red; Green; Blue }`). Per spec §Structs / §Enums, the canonical separators are `,` for struct fields and whitespace (newline) for enum variants. Fixed both, and re-laid-out `Shape` with one variant per line to match the spec's preferred format.
  - **§Declarations impl block** had `impl MyList for Team, Player { ... }` — the `for T1, T2` shape is not current grammar. Per spec §Interfaces and Ch 9's verified examples, multi-class impl blocks use angle-bracket arg lists: `impl MyList<Team, Player> { ... }`. Fixed.
  - **§Declarations impl block body** had `func P.add(self, child: C) { append(self.roster_children, child) }` — free-function `append` form is non-idiomatic per §Idiomatic Lyric, and the field reference `self.roster_children` bypasses the FieldBind line just above it (`P.children <-> Team.roster_children`). Inside the impl body the interface-facing name is `self.children`; the binding is what does the rename. Rewrote to `self.children.push(child)`.
  - **§Pattern Matching / Conditional extraction** used `if let Some(x) = optional_val { ... }` and `let Some(x) = optional_val else { return }` — but Lyric's `T?` is not modeled as `Some/None`; the spec's `if let` binding form for optionals is `if let x = optional_val { ... }` (line ~1431: "treats `expr` as optional and binds `name`"). Replaced the two `Some(x)` lines with the spec's actual forms, and added an `if let Circle(r) = shape` line to show the variant-destructuring form alongside. Also renamed the nested-destructuring example `Some(Inner(x))` → `Outer(Inner(x))` to avoid implying `Some` is built-in.
- **No removed features re-introduced.** Verified the appendix does not teach `defer`, `Mutex`/`Lock` (capital), `map[K]V`, the Go backend, CDD annotations (`why:`, `doc`, `invariant:`, etc.) as Lyric syntax, or `lyric verify`/`update`/`gen`. The `cascade` keyword appears only in the keyword table's 🚧-reserved row (correct framing) and now nowhere else.
- **Toolchain section verified against spec line 2340–2353:** `lyric compile file.ly`, `lyric compile <dir>`, `lyric compile --soa file.ly`, `lyric test file.ly`, `lyric fmt file.lyric`, `lyric help` — all current per spec §Compilation. The CDD-layer commands are correctly attributed to `lyre` with a forward pointer to Appendix E. No edit needed.
- **Known doc gap left in place (out of Appendix A scope):** Appendix B opens with "two files: `stdlib/std.ly` (618 lines) and `stdlib/string.ly` (259 lines)" — these numbers are stale per Ch 9 / Ch 14 (`std.ly` is now 733, `string.ly` 258, total 991). Belongs to whoever revises Appendix B.
- **Next units (App B, C, D, E):** no inheritance from Appendix A — it is a self-contained quick-ref. The decisions log entry below captures the one cross-cutting choice (`Some(x)` → unwrap-bind) in case another reference appendix repeats the pattern.

## Decision (App A): Optional binding spelled `if let x = optional_val`, not `if let Some(x) = optional_val`
Decided to fix Appendix A's "Conditional extraction" section to use the spec's actual binding form for `T?` (`if let x = opt { ... }`) instead of the Rust-style `if let Some(x) = opt { ... }`. Rationale: Lyric models optionals as `T?` (a nullable, not a `Some`/`None` enum); spec §`if let` (line 1426–1431) explicitly says `if let name = expr` "treats `expr` as optional and binds `name`", and `if let Variant(...) = expr` is the *enum*-destructuring form. The `Some(...)` spelling implies an enum that doesn't exist in stdlib (the user-rolled `Option` in §2.5 was a teaching scaffold, not a stdlib type). Same edit also renamed a nested-destructuring example from `Some(Inner(x))` to `Outer(Inner(x))` to avoid reinforcing the confusion. Next chapters / reference appendices: if you need to bind an optional payload, use `if let x = opt { ... }`; reserve `if let Variant(args) = e` for actual enum variants. If a user-defined `Option` enum is reintroduced anywhere, use that name explicitly, not `Some`.

### After App B
- **Calculator state unchanged.** Appendix B is reference material for the stdlib; no calculator code, no narrative.
- **Opener line counts refreshed:** `stdlib/std.ly` 618 → **733**, `stdlib/string.ly` 259 → **258**, total → **991**. Verified with `wc -l stdlib/*.ly`.
- **User-facing relation hints reduced from five to three.** The Appendix now documents exactly `ArrayList`, `DoublyLinked`, and `HashedList` as the user-facing relation hints. `OwningList` and `RefList` are removed from the user-facing surface — the `owns` vs `refs` keyword on the `relation` line is the cascade-vs-non-cascade switch (already true for `ArrayList`, intended to become true for `DoublyLinked` too). The internal stdlib factoring (`ArrayListBase`, the `OwningList`/`RefList` interfaces still embedded in `std.ly`, `RefArrayList`) is not exposed to readers. New TODO filed in `~/projects/lyric/TODO`: "Make `OwningList` and `RefList` private in the stdlib" — migrate `testdata/owning_list.ly` / `testdata/ref_list.ly` to `relation DoublyLinked X owns [Y]` / `relation DoublyLinked X refs [Y]`, then mark the two interfaces non-`pub`. Spec §Relations (lines 1247–1264 of `cr/docs/lyric-language-spec.md`) currently lists `OwningList` and `RefList` as user-facing — that section needs the same cut; flagged in the TODO entry.
- **`ArrayList` example converted to idiomatic method-call form** per Ch 8's "method-call form for every relation-generated op" decision: `array_append(t, p1)` → `t.roster_append(p1)`, `array_remove(p1)` → `t.roster_remove(p1)`, `len(t.roster_children)` → `t.roster_children.len()`.
- **`DoublyLinked` section now has its own user-facing entry** (was a "you don't use this directly" note). Includes a worked example excerpted from `testdata/graph.ly` showing three independent `DoublyLinked` relations on the same `Person` class, each on its own label (`nodes`, `out`, `in`) — the same pattern that motivates the label-prefix discipline in Ch 8.
- **`HashedList` field table** gained `P.hash_cap: i32` (was missing). Functions table extended with `hash_init(parent, cap)`. Added explicit 🚧 callout on "matches by hash alone" — `Hashable.equals` is still roadmap.
- **`Sym` section** added `.hash_key()` to the methods table (used by `HashedList`); kept `.equals(other)` and noted it's an external method (`pub func Sym.equals(self, other: Sym) -> bool` in `stdlib/std.ly`). Added prose on `pub permanent class SymTable { }` and what `permanent` does.
- **`Hashable` interface** kept as single-method (`get_hash`) with a 🚧 callout that `equals(other) -> bool` is on the roadmap. Added the fact that integer types (`i8`–`i64`, `u8`–`u64`) all implement `Hashable` per `stdlib/std.ly:483-490`.
- **`Dict<K, V>` methods table** gained `.length() -> i32` (the stdlib's actual name) with a 🚧 callout noting the spec/stdlib `len` vs `length` mismatch and recommending `d.keys().len()` as the portable form. Dict-literal subsection added: backtick-sym and integer-key forms taught as working, string-literal-key demoted to 🚧 with workaround pointer (matches Ch 10 reviser's decision).
- **`Error` section rewritten** to lead with the `Error { msg: "..." }` class literal as the idiomatic construction form (per Ch 5 decision), call out `f"{err}"` as the working stringification idiom (custom `message()` overrides are bypassed today — also Ch 5), and demote `new_error(msg)` to a 🚧 roadmap callout (checker accepts, C backend doesn't lower).
- **Conversion table fixed:** `itoa` is `i64 -> string` (was `i32`), `atoi` is `string -> (i64, bool)` (was `string -> i32`), added `parse_float` as a separate row, distinguished the C-backend builtins (`atoi`/`parse_float` with `(value, bool)` returns) from the stdlib lenient parsers (`parse_int`/`str_to_float` returning bare values with `0` on failure).
- **New §I/O Built-ins subsection added** before Appendix C. Inventories the current bootstrap I/O surface (`read_file`, `write_file`, `file_exists`, `list_dir`, `mkdtemp`, `exec_command`, `path_*` family, `os_args`/`os_exit`/`os_getwd`, `println`/`eprintln`/`panic`/`exit`) and adds a 🚧 callout pointing at the planned `Reader`/`Writer`/`File`/`BufferedReader` library from spec §I/O Library — Planned.
- **Sweep for removed features:** Appendix B does not teach `defer`, `cascade { body }`, `Mutex`/capital `Lock`, `map[K]V`, the Go backend, or CDD annotations as Lyric syntax; the word "cascade" appears only as a descriptive adjective ("cascade-destroyed") for `owns` behavior. `new_error` is documented under the Error section as 🚧 with the working alternative.
- **No empirical verification this pass.** Appendix B is reference, not narrative; the surface is read straight from `stdlib/std.ly` and `stdlib/string.ly`. The Ch 8 / Ch 9 / Ch 10 reviser sessions already verified the hint+method-form pattern end-to-end against `testdata/`.
- **Findings count:** 1 new TODO (make `OwningList`/`RefList` private).
- **Next units (App C, D, E):** no inheritance from App B — it's a self-contained stdlib reference. App C (toolchain), App D (Go/Rust/C++ → Lyric translation guide), and App E (lyre / CDD layer) are independent.

## Decision (App B): User-facing relation hints are `ArrayList`, `DoublyLinked`, `HashedList` only — `OwningList` and `RefList` are not user-facing
Decided to cut `OwningList` and `RefList` from Appendix B's "Relation Interfaces" section entirely, on Bill's direction. The user-facing relation surface is exactly three hints: `ArrayList` (dynamic array), `DoublyLinked` (intrusive doubly-linked list), `HashedList` (hash table). The `owns` vs `refs` keyword on the `relation` line selects cascade-vs-non-cascade destructor behavior — this is how `ArrayList` already works today, and it should be how `DoublyLinked` works too (the testdata files `owning_list.ly` and `ref_list.ly` predate this convention and use the separate interfaces directly). `OwningList` and `RefList` should become non-`pub` in `stdlib/std.ly` once the testdata is migrated and the `DoublyLinked` desugar pass synthesizes the cascade/non-cascade destructor variants from the `owns`/`refs` keyword. New TODO entry filed: "Make `OwningList` and `RefList` private in the stdlib." Same cut applies to spec §Relations (currently lists both as separate user-facing hints at lines 1247–1264) — flagged in the TODO. Next chapters / appendices: when documenting relation patterns, name only `ArrayList` / `DoublyLinked` / `HashedList`; do not introduce `OwningList` or `RefList` in user-facing examples.

## Decision (App B): Document only the user-facing stdlib surface; internal factoring stays invisible
Decided to keep Appendix B at the surface a Lyric user actually writes against: relation hints they name on a `relation` line, classes they instantiate (`Dict<K, V>`, `Sym`, `StringBuilder`, `Error`), interfaces they constrain on (`Hashable`, `error`), and free functions / builtins they call. The stdlib has internal factoring (`ArrayListBase` as the shared base for `ArrayList`/`RefArrayList`, the way `OwningList`/`RefList` embed `DoublyLinked`, etc.) that is real and visible in `stdlib/std.ly` source — but documenting it in a reference appendix would mislead readers into thinking those names are part of the API they should call. The appendix is a reference for *what to write*, not a tour of *how the stdlib is factored*; the latter belongs to a future "stdlib internals" appendix or a Ch 9 deep-dive. Next chapters / appendices: when in doubt about whether something belongs in App B, ask "would a user ever name this in their own code?" If no, leave it out.


### After App C
- **Calculator state unchanged.** Appendix C is toolchain reference material; no calculator code, no narrative. The Ch 5 `Parser` / `Token` / `TokenKind` and the §10.5 variable-bindings example are not touched and not referenced.
- **Toolchain surface synced to current reality:** documented exactly the four commands the self-hosted compiler ships with — `compile`, `test`, `fmt`, `help` (matches `src/main/main.ly` USAGE line and spec §Toolchain Commands at line ~2340). The pre-edit already correctly avoided `lyric verify` / `lyric update` / `lyric gen` (those moved to lyre — see Appendix E); no edit needed there.
- **§C.1 `lyric compile` flags row expanded.** Pre-edit signature only listed `--soa` and `--lir-dump`. Added `--detect-uaf`, `--rc-free`, `--no-rc` (all confirmed in `main.ly:716–722` and spec §Backend flags). Added per-flag prose paragraphs for each. Mentioned `--c` legacy no-op (per spec line 2360). The flag set now matches what the compiler actually accepts.
- **§C.1 Module mode corrected** in two ways:
  - (1) Pre-edit said module mode "discovers all `.ly` files in the directory tree, resolves imports between packages." That's overstated — `main.ly:735–765` walks only the top-level `.ly` files in one directory, and Ch 13 §13.7 finding establishes that imports are single-level (recursive imports are silently ignored). Rewrote to "walks the top-level `.ly` files in that directory and resolves the root file's `import` statements" with an explicit single-level callout pointing at Ch 13 §13.7.
  - (2) Pre-edit said "This is how the compiler compiles itself — `lyric compile . -o lyric.c` from the `src/` directory." False per Ch 13 §13.9 / Ch 14 §14.5: the compiler is built with the **flat file list** pattern (`./lyric compile $(BOOTSTRAP_FILES) -o lyric.c`), not module mode — `src/` has zero `import` statements between its 14 files. Rewrote to introduce the flat-file-list pattern explicitly as the working scale-up workaround, with cross-references to Chapters 13 §13.9 and 14 §14.5.
- **§C.2 `lyric test` rewritten** to drop the unverified `setjmp` / `longjmp` isolation claim (Ch 7 §7.3 reviser established that the runner has no per-test isolation; assertion failures call `exit(1)`, so the first failed assertion ends the suite). New prose says exactly that, with a 🚧 roadmap callout for per-test isolation / parallel execution / filtering / timing. Added a second 🚧 roadmap callout for the `assert` / `assert_eq` lowering still landing (per Ch 7 decision — Hewitt promised to fix the lowering before publication; until that lands, every test silently passes). Also documented the backend flags that `test` accepts (`--soa`, `--detect-uaf`, `--rc-free` / `--no-rc`, `--lir-dump`, `-o`) — confirmed in `main.ly:807–815`. The `-o` semantics (writes C and exits without running) are surfaced as a useful inspection mode.
- **§C.3 `lyric fmt` cross-reference fixed:** "design artifacts described in Chapter 14" → "Lyric artifacts described in Chapter 13 §13.8 — the same files consumed by the `lyre` toolchain documented in Appendix E." Chapter 13 §13.8 (per Ch 13 reviser's "lyre framing required in Ch 13" decision) is where `.lyric` mode is actually taught; Ch 14 is the compiler tour and doesn't introduce `.lyric` at all.
- **§C.4 `lyric help` block** matches `main.ly` USAGE string verbatim — no edit needed.
- **§C.5 numbers refreshed** to match Ch 14 / Ch 13 reviser figures: `30,796 lines of Lyric across 12 packages` → `33,500 lines of Lyric (the compiler in src/ plus the auto-imported stdlib/) across 14 files in 12 directories`; `105,457 lines of C` → `114,770 lines of C`; "0.2 seconds on a MacBook Air" → "a fraction of a second on a modern laptop" (the precise timing is unverifiable in this pass and not load-bearing — keep the qualitative point). Also softened the test-mode bullet from "the test harness" to "a generated runner that invokes each `test_*` function in source order" to match the §C.2 honest framing.
- **§C.6 The Bootstrap rewritten** to lead with the actual command anyone can run today — `make`, `make update`, `make self-test` / `bash test_self_compile.sh` — instead of the pre-edit's `lyric compile . -o bootstrap2.c` module-mode incantation (which doesn't match how the compiler is actually built; see the §C.1 module-mode fix above). The fixed-point property is preserved as the load-bearing pedagogical beat, but framed exactly the way Ch 14 §14.5 frames it: Stage 0 from checked-in `lyric.c`, Stage 2 self-compiles to `stage2.c`, Stage 2 binary compiles `src/` to `stage3.c`, `diff stage2.c stage3.c` must be empty. Cross-reference to Ch 14 §14.5 for the full story. `BOOTSTRAP_FILES` is named explicitly with a pointer back to Ch 13 §13.9 (the "no `import` statements" finding).
- **Sweep for removed features:** Appendix C does not teach `lyric verify` / `lyric update` / `lyric gen` (verified `grep` — clean), the `-mod` flag (verified clean), `defer`, `cascade { body }`, `Mutex` / capital `Lock`, `map[K]V`, the Go backend, or CDD annotations as Lyric syntax. The word "cascade" appears in §C.1's `--rc-free` paragraph as the descriptive verb "the owning relation cascades" — that's correct usage for the cascade-destruction *semantics*, not the removed `cascade { body }` statement.
- **No empirical verification this pass.** Appendix C is reference material — every claim was cross-checked against `src/main/main.ly` (canonical CLI source), `Makefile`, and the spec §Toolchain Commands / §Backend flags tables, but no end-to-end compile-and-run was performed because the appendix doesn't ship runnable Lyric snippets.
- **Findings count:** 0 new TODOs filed (the pre-existing TODOs the chapter references — `assert`/`assert_eq` lowering, per-test isolation — are already logged from Ch 7's revision).
- **Next units (App D, E):** no inheritance from App C. App D (Go/Rust/C++ translation guide) is a self-contained mapping table; App E (lyre / CDD layer) is also self-contained and is the canonical home for `lyric verify` / `update` / `gen` (which is why Appendix C only forward-points at it once via the `lyric fmt` chapter-reference). If App D or App E cites CLI commands, they should match the four-command surface (`compile`, `test`, `fmt`, `help`) — or, for lyre-side commands, name them explicitly as lyre subcommands.

## Decision (App C): Document the real four-command toolchain surface; forward-point lyre commands to Appendix E
Decided to lock Appendix C to exactly the commands the Lyric compiler ships with today: `compile`, `test`, `fmt`, `help`. The CDD-layer commands (`verify`, `update`, `gen`) live in `lyre` and are documented in Appendix E; Appendix C does not teach them or pretend they exist on `lyric`. The chapter's only forward pointer to lyre is in §C.3 (the `lyric fmt` paragraph), which names lyre and Appendix E once and moves on. Rationale: the spec is explicit at §Toolchain Commands line ~2363 — "The `lyric verify`, `lyric update`, and `lyric gen` commands are not in Lyric. They live in `lyre`." — and `main.ly:15` confirms the four-command USAGE string. Mixing the two surfaces in a single reference appendix has been a persistent source of reader confusion in older drafts; the boundary is now sharp. Next chapters / appendices: when documenting a CLI command, always check `main.ly` first; if it's not there, it belongs in Appendix E (lyre), not in the Lyric toolchain reference.

## Decision (App C): Bootstrap section uses `make` / `make update` / `make self-test` (or `bash test_self_compile.sh`), not module-mode self-compilation
Decided to rewrite §C.6 so the bootstrap demonstration uses the actual commands that work today: `make` (build from checked-in `lyric.c`), `make update` (regenerate `lyric.c` from `src/` via `./lyric compile $(BOOTSTRAP_FILES) -o lyric.c`), and `make self-test` / `bash test_self_compile.sh` (the four-stage fixed-point verification). The pre-edit's `lyric compile . -o bootstrap2.c` incantation was internally inconsistent — Ch 13 §13.9 / Ch 14 §14.5 / the actual `Makefile` all show the compiler is built with a flat file list, not module mode (because `src/` has zero `import` statements). The flat-file-list pattern is also documented as the canonical scale-up workaround in §C.1's module-mode section, so §C.6 now reinforces that pattern instead of contradicting it. Rationale: Bill's prompt explicitly named `make`, `make update`, `make test`, `make self-test`, and `bash test_self_compile.sh` as "the canonical build/test entrypoints," and Ch 14 §14.5 established the same framing as the book's load-bearing bootstrap-as-payoff beat. Next chapters / appendices: when citing the bootstrap, use the `make`-driven workflow; the fixed-point property is the pedagogical payoff, but the commands shown should be the ones a reader can paste into a terminal.

## Decision (App C): `lyric test` framed honestly — no `setjmp`/`longjmp` isolation, assertion lowering 🚧
Decided to fix §C.2's two false claims about the test runner: (a) "Each test runs in isolation using `setjmp`/`longjmp` — a failed `assert` or `assert_eq` jumps back to the test harness rather than crashing the process" — that's not what the runtime does today; the runtime header defines `lyric_assert` / `lyric_assert_eq` as macros that call `exit(1)` on failure, and the runner has no `setjmp` catch (Ch 7 §7.3 reviser empirical finding); (b) "If a test fails, the suite continues with the remaining tests" — false for the same reason. Rewrote the prose to match the reality (first failed assertion ends the suite, segfault also aborts) and added an explicit 🚧 roadmap callout for per-test isolation. Added a second 🚧 roadmap callout for the `assert` / `assert_eq` lowering — per Ch 7 decision, Hewitt intended to land the lowering before publication, but the appendix should be honest with readers about the current state in case they run a test suite that "passes" only because every assertion was dropped. Both callouts are framed in the standard *🚧 Roadmap: ...* italic-aside pattern from the §Permanent Invariants. Next chapters / appendices: when documenting test infrastructure, do not promise crash-isolation or continue-on-failure semantics until the runner gains them.

### After App D
- **Calculator state unchanged.** Appendix D is a translation guide (Go/Rust/C++ → Lyric concept-mapping tables); no calculator code, no narrative. The Ch 5 `Parser` / `Token` / `TokenKind` and the §10.5 variable-bindings example are not touched and not referenced.
- **D.5 "Default methods" row corrected.** Pre-edit showed `func P.count(self) -> i32 { ... }` (receiver-binding form inside an interface body) as the Lyric example. That declaration form parses, but the corresponding `obj.count()` call site fails to resolve for *user-defined* interfaces (Ch 9 §9.3 reviser finding — only the four stdlib hints get the label-prefixed method desugar). Replaced with the verified-working Ch 9 §9.1 shape: `func count(parent: P) -> i32 { ... }` (top-level default function inside the interface body), called as `count<P, C>(obj)` at the call site. Translation-table cell now matches a form that compiles end-to-end.
- **D.6 added "Scope-exit cleanup" row** mapping Go's `defer f.Close()` and Rust/C++ RAII to Lyric's scope-exit destructors plus `final func` escape hatch, with an explicit `**no `defer` keyword**` callout. Rationale: `defer` is the single most-asked-for Go feature when Go programmers cross over, and the carry-forward §Recently Removed lists `defer` as gone — leaving the table silent about it would force every Go reader to discover the absence by trying it and failing. The `final func` cross-reference also reinforces Ch 8 §8.7's introduction of that keyword.
- **D.6 "Destructor" row extended** to mention `final func cleanup(self)` for user code at destruction (alongside the auto-generated case), so the table presents both halves of the destruction story.
- **D.8 concurrency paragraph rewritten** to stop soft-pedaling the `spawn`-by-pointer data race. Pre-edit said "captures variables from the enclosing scope automatically — no explicit move or clone," which is technically accurate but actively misleading: the carry-forward §Permanent Invariants and Ch 12 §12.1 establish that pointer capture of any `let mut` value type is a textbook race. New paragraph names the race, names channels as the safe primitive (class pointers with internal locking), names `lock` from §12.5 and channel-funneling as the two workarounds, and ships a *🚧 Roadmap* italic-aside pointing at the copy-by-value spawn captures listed in spec §Roadmap §Concurrency. This is App D's most consequential edit — the translation guide is the place readers come *before* Ch 12, so it has to set honest expectations.
- **D.9 "Import" row corrected.** Pre-edit showed `import pkg` (the bare-keyword form). Per Ch 13 §13.6 / spec §Imports 🚧 list, bare `import "path"` is a known crasher and bare `import pkg` does not resolve a path. Fixed to `import pkg from "pkg"` — the `import alias from "path"` form is the only spelling that compiles end-to-end today (Ch 13 reviser verified). Matches the cross-package idiom Ch 13 §13.5 teaches.
- **D.10 `.lyric` bullet rewritten** from "design files with `why:`, `doc:`, `invariant:` annotations" (which framed CDD annotations as if they were Lyric features) to "sibling artifacts consumed by the **lyre** toolchain, which layers CDD annotations on top — these annotations are lyre features, not Lyric features." Two reasons: (a) carry-forward §Permanent Invariants is explicit that "CDD annotations are **lyre features, not Lyric features** — never document them as Lyric syntax", and a "What's New in Lyric" list is the worst place to break that rule; (b) the chapter reference was wrong — pre-edit pointed at Chapter 14 (the compiler tour, which doesn't introduce `.lyric` at all), where it should point at Ch 13 §13.8 (the language-side `.lyric` framing the Ch 13 reviser added) and Appendix E (lyre's home).
- **Sweep for removed Lyric syntax (clean):** Appendix D does not use `defer`, `cascade { body }`, `Mutex` / capital `Lock`, `map[K]V`, `fn(T) -> U`, `new_error(msg)`, or the Go backend anywhere. `lock` is correctly lowercase (D.8). `Dict<K, V>` is the hash-map mapping (D.7), not `map[K]V`. `Error { msg: "..." }` is the quick-error form (D.3), not `new_error(...)`. The `cascade` keyword appears only in the descriptive phrase "Cascade delete" (a behavior name, not the removed statement). The word "defer" appears once in the new D.6 row in a Go cell (`defer f.Close()`) and in the Lyric-cell callout that explicitly says no such keyword exists — both correct uses.
- **Sweep for idiomatic forms (clean post-edit):** method-call syntax everywhere (`ch.send(v)` / `ch.receive()`, `s[i]`, `x!`); `Error { msg: ... }` for error construction; `func` keyword for function-type and signature syntax; lowercase `lock`; `Dict<K, V>` for hash maps; f-string example in D.7; relations as the headline ownership story in D.6 + D.10.
- **No empirical verification this pass.** Appendix D is a concept-mapping table — no runnable Lyric snippets ship in it. Every Lyric cell was cross-checked against the spec's §Recently Removed, the carry-forward's §Permanent Invariants + §Idiomatic Lyric, and the prior chapter revisers' findings (Ch 5, Ch 8, Ch 9, Ch 12, Ch 13). The forms cited (`f"..."`, `T?`, `x!`, `(T, error)`, `?`, `<T: Comparable>`, `relation ... owns [...]`, `interface Graph<G, N, E>`, `field P.children: [C]`, `import alias from "path"`, `spawn { }`, `lock(mu) { }`) all match patterns verified end-to-end in earlier chapter revisions.
- **Findings count:** 0 new TODOs filed. Every issue surfaced in this pass was an editorial mapping against already-logged compiler bugs or already-decided idiomatic forms.
- **Next unit (App E):** no inheritance from App D — App D forward-points at App E for lyre, App E is independent. App E should be the canonical home for `lyric verify` / `lyric update` / `lyric gen` (named explicitly as lyre subcommands), the three-zone `.lyric` file layout, and the CDD annotation set (`why:`, `doc`, `invariant:`, `source:`, `fake:`). App D's D.10 `.lyric` bullet hands off to App E in exactly those words.

## Decision (App D): The translation guide tells the truth about `spawn`'s by-pointer capture
Decided to rewrite D.8's closing paragraph so the `spawn` cell is no longer soft-pedaled. Pre-edit said "`spawn` captures variables from the enclosing scope automatically — no explicit move or clone," which sounds like a benign feature but actively hides the textbook data race that Ch 12 §12.1 spends a prominent 🚧 callout on. New paragraph names the race, names channels as the safe primitive, names `lock` and channel-funneling as the two workarounds, and ships a *🚧 Roadmap* italic-aside pointing at the planned copy-by-value captures. Rationale: the translation guide is where Go/Rust/C++ programmers come *first* — before Ch 12 — to figure out how to translate `go func() { ... }()` into Lyric. If App D doesn't warn them, they'll write the obvious port (captured mutable counter) and ship the race. Carry-forward §Permanent Invariants on out-of-band orchestrator promises does not apply here — this is not aspirational, it's the current state, and the chapter pointer (Ch 12 §12.1) is intact. Next chapters / appendices: when documenting a primitive that has a known concurrency hazard, surface the hazard at the same level of prominence as the primitive itself, with a forward-pointer to the chapter that fully explains it.

## Decision (App D): CDD annotations never appear in a "What's New in Lyric" list — they belong to lyre
Decided to rewrite D.10's `.lyric` bullet from "design files with `why:`, `doc:`, `invariant:` annotations" to "sibling artifacts consumed by the **lyre** toolchain, which layers CDD annotations on top — these annotations are lyre features, not Lyric features." Two-line rationale: (a) carry-forward §Permanent Invariants explicitly says CDD annotations are "lyre features, not Lyric features — never document them as Lyric syntax," and the violation in App D was egregious because the bullet was inside a section titled "What's New — No Equivalent in Other Languages" *in the Lyric translation guide*, which framed the annotations as Lyric language features differentiating Lyric from Go/Rust/C++; (b) the chapter pointer was also wrong — pre-edit cited Chapter 14 (compiler tour, doesn't introduce `.lyric` at all), where it should cite Ch 13 §13.8 (the language-side `.lyric` framing the Ch 13 reviser added) and Appendix E (lyre's canonical home). Next chapters / appendices: whenever `.lyric` mode comes up, frame it as a sibling-artifact (declaration-only Lyric) and route any annotation-or-CDD discussion to Appendix E. Do not let CDD annotations appear in any Lyric-syntax inventory.

## Decision (App D): Bare `import pkg` is fiction; the working spelling is `import alias from "path"`
Decided to fix D.9's "Import" row from `import pkg` to `import pkg from "pkg"`. Bare `import pkg` does not resolve a path (the resolver needs a string literal); bare `import "path"` is a known crasher per Ch 13 §13.6 / spec §Imports 🚧. The only spelling that compiles end-to-end today is `import alias from "path"`, and Ch 13 §13.5 teaches it as the working cross-package idiom. Next chapters / appendices: any concept-mapping table that needs to show the Lyric import form should use the aliased-from spelling; the bare form remains 🚧 until the path-basename-as-alias derivation lands.

## Decision (App D): Add a "Scope-exit cleanup" row to D.6 mapping Go `defer` / Rust+C++ RAII to Lyric's `final func`
Decided to insert a new D.6 row covering scope-exit cleanup explicitly: Go's `defer f.Close()`, Rust's `impl Drop`, C++'s RAII destructors → Lyric's automatic scope-exit destructors + `final func cleanup(self)` escape hatch, with a bold `**no `defer` keyword**` callout. Rationale: `defer` is removed (spec §Recently Removed), but it is *the* canonical Go-to-Lyric translation question — every Go programmer crossing over asks "where did `defer` go?" within the first hour. Leaving the table silent would force every Go reader to discover the absence by trying it. The `final func` reference also gives the row pedagogical depth — it forward-points at Ch 8 §8.7 where `final func` is taught. The "Destructor" row was extended in the same edit to mention `final func` alongside the auto-generated form, so the two adjacent rows tell a coherent destruction story. Next chapters / appendices: when a removed feature is a "first-day question" for incoming users of a particular source language, the translation guide should answer it explicitly rather than leaving it for the §Recently Removed section to absorb.

### After App E
- **Book complete.** Appendix E is the final unit — there are no further chapters or appendices to hand off to. The book closes by routing all CDD/design-doc concerns to lyre.
- **Calculator state unchanged.** App E is a closer/pointer; no calculator code, no narrative changes. The Ch 5 `Parser` / `Token` / `TokenKind` and §10.5 variable-bindings example are not touched and not referenced.
- **Surgical edits (1 total replacement, expanding the closer from ~19 lines to ~31 lines):**
  - **Added multi-language framing paragraph.** The pre-edit App E mentioned lyre's multi-language scope only obliquely ("applies whether the implementation language is Lyric, Go, Python, or anything else `lyre` has an extractor for"). The post-edit names the four supported ecosystems explicitly (Lyric, Go, Python, TypeScript), names the actual extractor directories (`pkg/extract/golang`, `pkg/extract/python`, `pkg/extract/typescript`, `pkg/extract/lyric`), and adds the load-bearing inversion: "Lyric is one of lyre's targets, not its only one." This matches the preface's "A sibling artifact: lyre" subsection (which already establishes lyre as multi-language) and Ch 13 §13.8 (which says "one implementation per language ecosystem"). A reader who jumps straight to App E now gets the multi-language picture without needing to flip back to the preface.
  - **Added lyre's command-surface table.** Pre-edit listed only `lyre verify` / `lyre update` / `lyre gen` in a prose mention. Post-edit ships a five-row table covering `gen`, `update`, `verify`, `lint`, `fmt` — the full surface that `./lyre help` reports today (verified empirically: `cd ~/projects/lyre && ./lyre help` prints exactly these five subcommands with these descriptions). The `lint` and `fmt` commands were missing from the pre-edit entirely. A one-line "no `lyre compile`, no `lyre run`" disclaimer makes the boundary between lyre (artifacts) and lyric/go/python/tsc (compilation) explicit.
  - **Sharpened the format-vs-tool split.** Pre-edit's "What stays in Lyric proper" bullets were correct but unclear about *why* the `.lyric` format is a Lyric concept while CDD is a lyre concept. Post-edit reframes the format bullet as "lyre piggybacks on the Lyric grammar to keep `.lyric` files trivially parseable for the Lyric-implementation case" — which both explains the relationship and reinforces that the multi-language extractors produce files lyre also handles but Lyric proper doesn't need to parse.
  - **Closer tightened to a forward-pointer.** Pre-edit ended with "The Lyric toolchain ships four subcommands — `compile`, `test`, `fmt`, `help` — and nothing else. The verify/update/gen commands you may have seen in earlier drafts live in `lyre`." That duplicated App C's own four-command table (App C §C.4 + §C.5). Post-edit closes with "The Lyric toolchain itself ships exactly the four subcommands documented in Appendix C; everything in this appendix belongs to lyre" — same correctness, no duplication, and a clean cross-reference back to the canonical home.
  - **Backticked-`lyre` → bold `lyre` for consistency.** Pre-edit alternated between `lyre` (code-font) and prose mentions; post-edit uses **lyre** (bold) for the tool-name on first mention and bare `lyre` (no backticks) for prose mentions, matching the carry-forward §Permanent Invariants framing ("**lyre** is a separate toolchain"). Command-form invocations (`lyre gen`, `lyre verify`, etc.) keep their backticks.
- **Header retained verbatim:** `## Appendix E: The CDD Layer (`lyre`)`. The book's outer ToC and the App D forward-pointer (D.10 `.lyric` bullet) both reference this header.
- **What was *not* changed and why:**
  - The opening "Earlier drafts described CDD as if it were part of the Lyric language" framing is retained — it's load-bearing for any reader carrying expectations from earlier book versions, and it correctly orients new readers to the format-vs-tool split.
  - The `guarded_by(lock_name)` mention (the one annotation Lyric's own grammar parses) and the function-level-annotation roadmap mention are kept verbatim. Both are accurate per Ch 12 and the spec §Roadmap respectively.
  - The pointer to the lyre repository is kept (without the literal path `~/projects/lyre/`, which was orchestrator-specific). Generic "see the lyre repository and its own documentation" is the right level for a published book.
- **Sweep for removed features / framing errors (all clean):** App E does not teach CDD annotations as Lyric syntax (it explicitly attributes them to lyre); does not claim `lyric verify` / `update` / `gen` exist on `lyric` (correctly attributes all three to `lyre`); does not duplicate App C's four-command surface inventory (forward-points instead); does not frame lyre as Lyric-only (multi-language reach is explicit). The word "cascade" does not appear; `defer`, `Mutex`/capital `Lock`, `map[K]V`, the Go backend, `fn(T) -> U`, `new_error(msg)` — none appear.
- **Empirical verification:** `cd ~/projects/lyre && ./lyre help` confirms the five-command surface (`gen`, `update`, `verify`, `lint`, `fmt`) matches the table verbatim. `ls ~/projects/lyre/pkg/extract/` confirms the four extractor directories (`golang`, `python`, `typescript`, `lyric`). No book code blocks ship in App E (it's a closer, not a tutorial), so no compile-and-run cycles needed.
- **Findings count:** 0 new TODOs filed. App E references the same `guarded_by` and function-level-annotation roadmap items already on file from Ch 12 / spec §Roadmap.

## Decision (App E): App E names the full lyre command surface (gen / update / verify / lint / fmt); pre-edit's three-command list was incomplete
Decided to ship a five-row table covering every subcommand `./lyre help` reports today, not just the three (`verify`, `update`, `gen`) the pre-edit mentioned in prose. Empirical: `lyre lint` (reports recoverable quality issues like missing `why:`, broken `source:` links, stale `verified_at:`) and `lyre fmt` (formats `.lyric` files using Lyric grammar) both exist and are user-facing; omitting them from App E would have left readers searching for `lyre lint` and finding nothing. Format is a 2-column table (Command | Purpose) consistent with App C's CLI tables and App D's concept-mapping tables. Rationale for the table form over prose: App E is a closer that has to orient a reader in one screen, and a table is the densest format that still names every command. Next chapters / appendices: none — App E is the final unit. If lyre grows additional subcommands, this table is the single source of truth in the book; update it alongside lyre's own help output. The book intentionally does *not* try to document each subcommand's flags or behavior in depth — that belongs to lyre's own docs, and App E's job is to point readers there.

## Decision (App E): App E foregrounds lyre's multi-language reach (Lyric, Go, Python, TypeScript) so a reader who jumps straight to the appendix gets the picture
Decided to add an explicit multi-language paragraph naming the four supported source ecosystems (Lyric, Go, Python, TypeScript) and the four extractor directories (`pkg/extract/golang`, `pkg/extract/python`, `pkg/extract/typescript`, `pkg/extract/lyric`), closing with the load-bearing inversion "Lyric is one of lyre's targets, not its only one." Rationale: the preface and Ch 13 §13.8 both establish lyre's multi-language nature, but App E is where a reader looking for CDD docs will land first (the App D D.10 `.lyric` bullet hands off directly to App E with no preface detour). If App E itself doesn't say "Go, Python, TypeScript, Lyric" out loud, readers will infer lyre is Lyric-specific — which is exactly the misframing the carry-forward §Permanent Invariants warned against ("lyre is a separate toolchain (Go/Python/TypeScript/Lyric) that reads `.lyric` files and verifies them against implementations"). The directory names are cited verbatim from a fresh `ls ~/projects/lyre/pkg/extract/`. Next chapters / appendices: none — App E is the final unit. Any future book edition that adds new extractors should update both this paragraph and the §13.8 sibling paragraph; they're the two load-bearing mentions of the multi-language list.
